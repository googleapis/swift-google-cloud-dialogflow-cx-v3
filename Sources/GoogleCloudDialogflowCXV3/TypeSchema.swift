// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if Flows || Playbooks || Sessions || TestCases
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleCloudWkt

  /// Encapsulates different type schema variations: either a reference to an
  /// a schema that's already defined by a tool, or an inline definition.
  public struct TypeSchema: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// The encapsulated schema.
    public var schema: OneOf_Schema? = nil

    /// Initialize a new instance of `TypeSchema`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = TypeSchema().with { $0.inlineSchema = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case inlineSchema = "inlineSchema"
      case schemaReference = "schemaReference"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)

      var schema: OneOf_Schema? = nil
      let schemaCheckAndSet = {
        if schema != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'schema'"))
        }
        schema = $0
      }
      if let inlineSchema = try container.decodeIfPresent(InlineSchema?.self, forKey: .inlineSchema)
      {
        try schemaCheckAndSet(.inlineSchema(inlineSchema))
      }
      if let schemaReference = try container.decodeIfPresent(
        TypeSchema.SchemaReference?.self, forKey: .schemaReference)
      {
        try schemaCheckAndSet(.schemaReference(schemaReference))
      }
      self.schema = schema
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)

      if let choice = self.schema {
        switch choice {
        case .inlineSchema(let value):
          try container.encode(value, forKey: .inlineSchema)
        case .schemaReference(let value):
          try container.encode(value, forKey: .schemaReference)
        }
      }
    }

    /// A reference to the schema of an existing tool.
    public struct SchemaReference: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// The tool that contains this schema definition.
      /// Format:
      /// `projects/<ProjectID>/locations/<LocationID>/agents/<AgentID>/tools/<ToolID>`.
      public var tool: Swift.String = Swift.String()

      /// The name of the schema.
      public var schema: Swift.String = Swift.String()

      /// Initialize a new instance of `SchemaReference`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = SchemaReference().with { $0.tool = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.TypeSchema.SchemaReference"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// The encapsulated schema.
    public enum OneOf_Schema: Codable, Equatable, Sendable {
      /// Set if this is an inline schema definition.
      indirect case inlineSchema(InlineSchema?)
      /// Set if this is a schema reference.
      indirect case schemaReference(TypeSchema.SchemaReference?)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dialogflow.cx.v3.TypeSchema"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
