// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if Agents || Flows
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleCloudWKT

  /// Agent/flow validation message.
  public struct ValidationMessage: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The type of the resources where the message is found.
    public var resourceType: ValidationMessage.ResourceType = ValidationMessage.ResourceType()

    /// The names of the resources where the message is found.
    @available(*, deprecated)
    public var resources: [Swift.String] = []

    /// The resource names of the resources where the message is found.
    public var resourceNames: [ResourceName] = []

    /// Indicates the severity of the message.
    public var severity: ValidationMessage.Severity = ValidationMessage.Severity()

    /// The message detail.
    public var detail: Swift.String = Swift.String()

    /// Initialize a new instance of `ValidationMessage`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ValidationMessage().with { $0.resourceType = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Resource types.
    public enum ResourceType: Codable, Equatable, Sendable {
      /// Unspecified.
      case unspecified
      /// Agent.
      case agent
      /// Intent.
      case intent
      /// Entity type.
      case entityType
      /// Webhook.
      case webhook
      /// Flow.
      case flow
      /// Page.
      case page
      /// Transition route group.
      case transitionRouteGroup
      /// Intent training phrase.
      case intentTrainingPhrase
      /// Intent parameter.
      case intentParameter
      /// Multiple intents.
      case intents
      /// Multiple training phrases.
      case intentTrainingPhrases
      /// Multiple entity types.
      case entityTypes
      /// Multiple pages.
      case pages
      /// Agent transition route group.
      case agentTransitionRouteGroup
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .agent: return 1
        case .intent: return 2
        case .entityType: return 3
        case .webhook: return 4
        case .flow: return 5
        case .page: return 6
        case .transitionRouteGroup: return 7
        case .intentTrainingPhrase: return 8
        case .intentParameter: return 9
        case .intents: return 10
        case .intentTrainingPhrases: return 11
        case .entityTypes: return 12
        case .pages: return 13
        case .agentTransitionRouteGroup: return 14
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "RESOURCE_TYPE_UNSPECIFIED"
        case .agent: return "AGENT"
        case .intent: return "INTENT"
        case .entityType: return "ENTITY_TYPE"
        case .webhook: return "WEBHOOK"
        case .flow: return "FLOW"
        case .page: return "PAGE"
        case .transitionRouteGroup: return "TRANSITION_ROUTE_GROUP"
        case .intentTrainingPhrase: return "INTENT_TRAINING_PHRASE"
        case .intentParameter: return "INTENT_PARAMETER"
        case .intents: return "INTENTS"
        case .intentTrainingPhrases: return "INTENT_TRAINING_PHRASES"
        case .entityTypes: return "ENTITY_TYPES"
        case .pages: return "PAGES"
        case .agentTransitionRouteGroup: return "AGENT_TRANSITION_ROUTE_GROUP"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ResourceType/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "RESOURCE_TYPE_UNSPECIFIED": self = .unspecified
        case "AGENT": self = .agent
        case "INTENT": self = .intent
        case "INTENT_TRAINING_PHRASE": self = .intentTrainingPhrase
        case "INTENT_PARAMETER": self = .intentParameter
        case "INTENTS": self = .intents
        case "INTENT_TRAINING_PHRASES": self = .intentTrainingPhrases
        case "ENTITY_TYPE": self = .entityType
        case "ENTITY_TYPES": self = .entityTypes
        case "WEBHOOK": self = .webhook
        case "FLOW": self = .flow
        case "PAGE": self = .page
        case "PAGES": self = .pages
        case "TRANSITION_ROUTE_GROUP": self = .transitionRouteGroup
        case "AGENT_TRANSITION_ROUTE_GROUP": self = .agentTransitionRouteGroup
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ResourceType/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .agent
        case 2: self = .intent
        case 3: self = .entityType
        case 4: self = .webhook
        case 5: self = .flow
        case 6: self = .page
        case 7: self = .transitionRouteGroup
        case 8: self = .intentTrainingPhrase
        case 9: self = .intentParameter
        case 10: self = .intents
        case 11: self = .intentTrainingPhrases
        case 12: self = .entityTypes
        case 13: self = .pages
        case 14: self = .agentTransitionRouteGroup
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .agent: return try container.encode(1)
        case .intent: return try container.encode(2)
        case .entityType: return try container.encode(3)
        case .webhook: return try container.encode(4)
        case .flow: return try container.encode(5)
        case .page: return try container.encode(6)
        case .transitionRouteGroup: return try container.encode(7)
        case .intentTrainingPhrase: return try container.encode(8)
        case .intentParameter: return try container.encode(9)
        case .intents: return try container.encode(10)
        case .intentTrainingPhrases: return try container.encode(11)
        case .entityTypes: return try container.encode(12)
        case .pages: return try container.encode(13)
        case .agentTransitionRouteGroup: return try container.encode(14)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Severity level.
    public enum Severity: Codable, Equatable, Sendable {
      /// Unspecified.
      case unspecified
      /// The agent doesn't follow Dialogflow best practices.
      case info
      /// The agent may not behave as expected.
      case warning
      /// The agent may experience failures.
      case error
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .info: return 1
        case .warning: return 2
        case .error: return 3
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "SEVERITY_UNSPECIFIED"
        case .info: return "INFO"
        case .warning: return "WARNING"
        case .error: return "ERROR"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Severity/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "SEVERITY_UNSPECIFIED": self = .unspecified
        case "INFO": self = .info
        case "WARNING": self = .warning
        case "ERROR": self = .error
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Severity/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .info
        case 2: self = .warning
        case 3: self = .error
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .info: return try container.encode(1)
        case .warning: return try container.encode(2)
        case .error: return try container.encode(3)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dialogflow.cx.v3.ValidationMessage"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }
#endif
