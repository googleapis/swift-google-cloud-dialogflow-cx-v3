// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if EntityTypes
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleCloudWKT

  /// The request message for
  /// [EntityTypes.ExportEntityTypes][google.cloud.dialogflow.cx.v3.EntityTypes.ExportEntityTypes].
  ///
  /// [google.cloud.dialogflow.cx.v3.EntityTypes.ExportEntityTypes]: <doc:EntityTypesClient/exportEntityTypes(request:options:)>
  public struct ExportEntityTypesRequest: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Required. The name of the parent agent to export entity types.
    /// Format: `projects/<ProjectID>/locations/<LocationID>/agents/<AgentID>`.
    public var parent: Swift.String = Swift.String()

    /// Required. The name of the entity types to export.
    /// Format:
    /// `projects/<ProjectID>/locations/<LocationID>/agents/<AgentID>/entityTypes/<EntityTypeID>`.
    public var entityTypes: [Swift.String] = []

    /// Optional. The data format of the exported entity types. If not specified,
    /// `BLOB` is assumed.
    public var dataFormat: ExportEntityTypesRequest.DataFormat =
      ExportEntityTypesRequest.DataFormat()

    /// Optional. The language to retrieve the entity type for. The following
    /// fields are language dependent:
    ///
    /// *   `EntityType.entities.value`
    /// *   `EntityType.entities.synonyms`
    /// *   `EntityType.excluded_phrases.value`
    ///
    /// If not specified, all language dependent fields will be retrieved.
    /// [Many
    /// languages](https://cloud.google.com/dialogflow/docs/reference/language)
    /// are supported.
    /// Note: languages must be enabled in the agent before they can be used.
    public var languageCode: Swift.String = Swift.String()

    /// The destination to export.
    public var destination: OneOf_Destination? = nil

    /// Initialize a new instance of `ExportEntityTypesRequest`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ExportEntityTypesRequest().with { $0.parent = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case parent = "parent"
      case entityTypes = "entityTypes"
      case entityTypesUri = "entityTypesUri"
      case entityTypesContentInline = "entityTypesContentInline"
      case dataFormat = "dataFormat"
      case languageCode = "languageCode"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.parent = try container.decode(Swift.String.self, forKey: .parent)
      self.entityTypes = try container.decode([Swift.String].self, forKey: .entityTypes)
      self.dataFormat = try container.decode(
        ExportEntityTypesRequest.DataFormat.self, forKey: .dataFormat)
      self.languageCode = try container.decode(Swift.String.self, forKey: .languageCode)

      var destination: OneOf_Destination? = nil
      let destinationCheckAndSet = {
        if destination != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'destination'"))
        }
        destination = $0
      }
      if let entityTypesUri = try container.decodeIfPresent(
        Swift.String.self, forKey: .entityTypesUri)
      {
        try destinationCheckAndSet(.entityTypesUri(entityTypesUri))
      }
      if let entityTypesContentInline = try container.decodeIfPresent(
        Swift.Bool.self, forKey: .entityTypesContentInline)
      {
        try destinationCheckAndSet(.entityTypesContentInline(entityTypesContentInline))
      }
      self.destination = destination
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.parent, forKey: .parent)
      try container.encode(self.entityTypes, forKey: .entityTypes)
      try container.encode(self.dataFormat, forKey: .dataFormat)
      try container.encode(self.languageCode, forKey: .languageCode)

      if let choice = self.destination {
        switch choice {
        case .entityTypesUri(let value):
          try container.encode(value, forKey: .entityTypesUri)
        case .entityTypesContentInline(let value):
          try container.encode(value, forKey: .entityTypesContentInline)
        }
      }
    }

    /// Data format of the exported entity types.
    public enum DataFormat: Codable, Equatable, Sendable {
      /// Unspecified format. Treated as `BLOB`.
      case unspecified
      /// EntityTypes will be exported as raw bytes.
      case blob
      /// EntityTypes will be exported in JSON Package format.
      case jsonPackage
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .blob: return 1
        case .jsonPackage: return 5
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "DATA_FORMAT_UNSPECIFIED"
        case .blob: return "BLOB"
        case .jsonPackage: return "JSON_PACKAGE"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:DataFormat/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "DATA_FORMAT_UNSPECIFIED": self = .unspecified
        case "BLOB": self = .blob
        case "JSON_PACKAGE": self = .jsonPackage
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:DataFormat/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .blob
        case 5: self = .jsonPackage
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .blob: return try container.encode(1)
        case .jsonPackage: return try container.encode(5)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// The destination to export.
    public enum OneOf_Destination: Codable, Equatable, Sendable {
      /// Optional. The [Google Cloud
      /// Storage](https://cloud.google.com/storage/docs/) URI to export the entity
      /// types to. The format of this URI must be
      /// `gs://<bucket-name>/<object-name>`.
      ///
      /// Dialogflow performs a write operation for the Cloud Storage object
      /// on the caller's behalf, so your request authentication must
      /// have write permissions for the object. For more information, see
      /// [Dialogflow access
      /// control](https://cloud.google.com/dialogflow/cx/docs/concept/access-control#storage).
      case entityTypesUri(Swift.String)
      /// Optional. The option to return the serialized entity types inline.
      case entityTypesContentInline(Swift.Bool)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dialogflow.cx.v3.ExportEntityTypesRequest"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }
#endif
