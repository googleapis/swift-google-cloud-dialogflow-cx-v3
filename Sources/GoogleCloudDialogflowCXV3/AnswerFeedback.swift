// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if Sessions
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleCloudWkt

  /// Stores information about feedback provided by users about a response.
  public struct AnswerFeedback: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. Rating from user for the specific Dialogflow response.
    public var rating: AnswerFeedback.Rating = AnswerFeedback.Rating()

    /// Optional. In case of thumbs down rating provided, users can optionally
    /// provide context about the rating.
    public var ratingReason: AnswerFeedback.RatingReason? = nil

    /// Optional. Custom rating from the user about the provided answer, with
    /// maximum length of 1024 characters. For example, client could use a
    /// customized JSON object to indicate the rating.
    public var customRating: Swift.String = Swift.String()

    /// Initialize a new instance of `AnswerFeedback`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = AnswerFeedback().with { $0.rating = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Stores extra information about why users provided thumbs down rating.
    public struct RatingReason: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Optional. Custom reason labels for thumbs down rating provided by the
      /// user. The maximum number of labels allowed is 10 and the maximum length
      /// of a single label is 128 characters.
      public var reasonLabels: [Swift.String] = []

      /// Optional. Additional feedback about the rating.
      /// This field can be populated without choosing a predefined `reason`.
      public var feedback: Swift.String = Swift.String()

      /// Initialize a new instance of `RatingReason`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = RatingReason().with { $0.reasonLabels = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.AnswerFeedback.RatingReason"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Represents thumbs up/down rating provided by user about a response.
    public enum Rating: Codable, Equatable, Sendable {
      /// Rating not specified.
      case unspecified
      /// Thumbs up feedback from user.
      case thumbsUp
      /// Thumbs down feedback from user.
      case thumbsDown
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .thumbsUp: return 1
        case .thumbsDown: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "RATING_UNSPECIFIED"
        case .thumbsUp: return "THUMBS_UP"
        case .thumbsDown: return "THUMBS_DOWN"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Rating/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "RATING_UNSPECIFIED": self = .unspecified
        case "THUMBS_UP": self = .thumbsUp
        case "THUMBS_DOWN": self = .thumbsDown
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Rating/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .thumbsUp
        case 2: self = .thumbsDown
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .thumbsUp: return try container.encode(1)
        case .thumbsDown: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dialogflow.cx.v3.AnswerFeedback"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
