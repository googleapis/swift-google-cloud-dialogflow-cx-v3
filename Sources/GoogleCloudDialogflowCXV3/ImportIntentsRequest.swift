// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if Intents
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleWKT

  /// The request message for
  /// [Intents.ImportIntents][google.cloud.dialogflow.cx.v3.Intents.ImportIntents].
  ///
  /// [google.cloud.dialogflow.cx.v3.Intents.ImportIntents]: <doc:IntentsClient/importIntents(request:options:)>
  public struct ImportIntentsRequest: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Required. The agent to import the intents into.
    /// Format: `projects/<ProjectID>/locations/<LocationID>/agents/<AgentID>`.
    public var parent: Swift.String = Swift.String()

    /// Merge option for importing intents. If not specified, `REJECT` is assumed.
    public var mergeOption: ImportIntentsRequest.MergeOption = ImportIntentsRequest.MergeOption()

    /// Required. The intents to import.
    public var intents: OneOf_Intents? = nil

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `ImportIntentsRequest`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ImportIntentsRequest().with { $0.parent = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let parent = CodingKeys(stringValue: "parent")
      static let intentsUri = CodingKeys(stringValue: "intentsUri")
      static let intentsContent = CodingKeys(stringValue: "intentsContent")
      static let mergeOption = CodingKeys(stringValue: "mergeOption")

      static let _knownKeys: Set<Swift.String> = [
        "parent",
        "intentsUri",
        "intentsContent",
        "mergeOption",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .parent) {
        self.parent = value
      }
      if let value = try container.decodeIfPresent(
        ImportIntentsRequest.MergeOption.self, forKey: .mergeOption)
      {
        self.mergeOption = value
      }

      var intents: OneOf_Intents? = nil
      let intentsCheckAndSet = {
        if intents != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'intents'"))
        }
        intents = $0
      }
      if let intentsUri = try container.decodeIfPresent(Swift.String.self, forKey: .intentsUri) {
        try intentsCheckAndSet(.intentsUri(intentsUri))
      }
      if let intentsContent = try container.decodeIfPresent(
        InlineSource?.self, forKey: .intentsContent)
      {
        try intentsCheckAndSet(.intentsContent(intentsContent))
      }
      self.intents = intents
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.parent, forKey: .parent)
      try container.encode(self.mergeOption, forKey: .mergeOption)

      if let choice = self.intents {
        switch choice {
        case .intentsUri(let value):
          try container.encode(value, forKey: .intentsUri)
        case .intentsContent(let value):
          try container.encode(value, forKey: .intentsContent)
        }
      }
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    /// Merge option when display name conflicts exist during import.
    ///
    /// - Note: Adding cases to this enumeration is not considered a breaking change.
    ///   Always include an `@unknown default:` case when switching over this type.
    ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
    ///   expecting specific values to remain unparsed; future releases may promote
    ///   them to named cases.
    public enum MergeOption: Codable, Equatable, Sendable {
      /// Unspecified. Should not be used.
      case unspecified
      /// DEPRECATED: Please use
      /// [REPORT_CONFLICT][ImportIntentsRequest.REPORT_CONFLICT] instead.
      /// Fail the request if there are intents whose display names conflict with
      /// the display names of intents in the agent.
      @available(*, deprecated)
      case reject
      /// Replace the original intent in the agent with the new intent when display
      /// name conflicts exist.
      case replace
      /// Merge the original intent with the new intent when display name conflicts
      /// exist.
      case merge
      /// Create new intents with new display names to differentiate them from the
      /// existing intents when display name conflicts exist.
      case rename
      /// Report conflict information if display names conflict is detected.
      /// Otherwise, import intents.
      case reportConflict
      /// Keep the original intent and discard the conflicting new intent when
      /// display name conflicts exist.
      case keep
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown value is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      ///
      /// - Warning: Do not pattern-match specific integer values in this case;
      ///   future releases may promote them to named enum cases.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown value is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      ///
      /// - Warning: Do not pattern-match specific string literals in this case;
      ///   future releases may promote them to named enum cases.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .reject: return 1
        case .replace: return 2
        case .merge: return 3
        case .rename: return 4
        case .reportConflict: return 5
        case .keep: return 6
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "MERGE_OPTION_UNSPECIFIED"
        case .reject: return "REJECT"
        case .replace: return "REPLACE"
        case .merge: return "MERGE"
        case .rename: return "RENAME"
        case .reportConflict: return "REPORT_CONFLICT"
        case .keep: return "KEEP"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:MergeOption/unknownStringValue(_:)).
      #if hasAttribute(diagnose)
        @diagnose(DeprecatedDeclaration, as: ignored)
      #endif
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "MERGE_OPTION_UNSPECIFIED": self = .unspecified
        case "REJECT": self = .reject
        case "REPLACE": self = .replace
        case "MERGE": self = .merge
        case "RENAME": self = .rename
        case "REPORT_CONFLICT": self = .reportConflict
        case "KEEP": self = .keep
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:MergeOption/unknownIntValue(_:)).
      #if hasAttribute(diagnose)
        @diagnose(DeprecatedDeclaration, as: ignored)
      #endif
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .reject
        case 2: self = .replace
        case 3: self = .merge
        case 4: self = .rename
        case 5: self = .reportConflict
        case 6: self = .keep
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode("MERGE_OPTION_UNSPECIFIED")
        case .reject: return try container.encode("REJECT")
        case .replace: return try container.encode("REPLACE")
        case .merge: return try container.encode("MERGE")
        case .rename: return try container.encode("RENAME")
        case .reportConflict: return try container.encode("REPORT_CONFLICT")
        case .keep: return try container.encode("KEEP")
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Required. The intents to import.
    public enum OneOf_Intents: Codable, Equatable, Sendable {
      /// The [Google Cloud Storage](https://cloud.google.com/storage/docs/) URI
      /// to import intents from. The format of this URI must be
      /// `gs://<bucket-name>/<object-name>`.
      ///
      /// Dialogflow performs a read operation for the Cloud Storage object
      /// on the caller's behalf, so your request authentication must
      /// have read permissions for the object. For more information, see
      /// [Dialogflow access
      /// control](https://cloud.google.com/dialogflow/cx/docs/concept/access-control#storage).
      case intentsUri(Swift.String)
      /// Uncompressed byte content of intents.
      indirect case intentsContent(InlineSource?)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dialogflow.cx.v3.ImportIntentsRequest"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }
#endif
