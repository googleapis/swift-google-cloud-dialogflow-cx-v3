// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if Experiments
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleWKT

  /// Represents an experiment in an environment.
  public struct Experiment: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// The name of the experiment.
    /// Format:
    /// projects/<ProjectID>/locations/<LocationID>/agents/<AgentID>/environments/<EnvironmentID>/experiments/<ExperimentID>.
    public var name: Swift.String = Swift.String()

    /// Required. The human-readable name of the experiment (unique in an
    /// environment). Limit of 64 characters.
    public var displayName: Swift.String = Swift.String()

    /// The human-readable description of the experiment.
    public var description: Swift.String = Swift.String()

    /// The current state of the experiment.
    /// Transition triggered by Experiments.StartExperiment: DRAFT->RUNNING.
    /// Transition triggered by Experiments.CancelExperiment: DRAFT->DONE or
    /// RUNNING->DONE.
    public var state: Experiment.State = Experiment.State()

    /// The definition of the experiment.
    public var definition: Experiment.Definition? = nil

    /// The configuration for auto rollout. If set, there should be exactly two
    /// variants in the experiment (control variant being the default version of
    /// the flow), the traffic allocation for the non-control variant will
    /// gradually increase to 100% when conditions are met, and eventually
    /// replace the control variant to become the default version of the flow.
    public var rolloutConfig: RolloutConfig? = nil

    /// State of the auto rollout process.
    public var rolloutState: RolloutState? = nil

    /// The reason why rollout has failed. Should only be set when state is
    /// ROLLOUT_FAILED.
    public var rolloutFailureReason: Swift.String = Swift.String()

    /// Inference result of the experiment.
    public var result: Experiment.Result? = nil

    /// Creation time of this experiment.
    public var createTime: GoogleWKT.Timestamp? = nil

    /// Start time of this experiment.
    public var startTime: GoogleWKT.Timestamp? = nil

    /// End time of this experiment.
    public var endTime: GoogleWKT.Timestamp? = nil

    /// Last update time of this experiment.
    public var lastUpdateTime: GoogleWKT.Timestamp? = nil

    /// Maximum number of days to run the experiment/rollout. If auto-rollout is
    /// not enabled, default value and maximum will be 30 days. If auto-rollout is
    /// enabled, default value and maximum will be 6 days.
    public var experimentLength: GoogleWKT.Duration? = nil

    /// The history of updates to the experiment variants.
    public var variantsHistory: [VariantsHistory] = []

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `Experiment`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Experiment().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let name = CodingKeys(stringValue: "name")
      static let displayName = CodingKeys(stringValue: "displayName")
      static let description = CodingKeys(stringValue: "description")
      static let state = CodingKeys(stringValue: "state")
      static let definition = CodingKeys(stringValue: "definition")
      static let rolloutConfig = CodingKeys(stringValue: "rolloutConfig")
      static let rolloutState = CodingKeys(stringValue: "rolloutState")
      static let rolloutFailureReason = CodingKeys(stringValue: "rolloutFailureReason")
      static let result = CodingKeys(stringValue: "result")
      static let createTime = CodingKeys(stringValue: "createTime")
      static let startTime = CodingKeys(stringValue: "startTime")
      static let endTime = CodingKeys(stringValue: "endTime")
      static let lastUpdateTime = CodingKeys(stringValue: "lastUpdateTime")
      static let experimentLength = CodingKeys(stringValue: "experimentLength")
      static let variantsHistory = CodingKeys(stringValue: "variantsHistory")

      static let _knownKeys: Set<Swift.String> = [
        "name",
        "displayName",
        "description",
        "state",
        "definition",
        "rolloutConfig",
        "rolloutState",
        "rolloutFailureReason",
        "result",
        "createTime",
        "startTime",
        "endTime",
        "lastUpdateTime",
        "experimentLength",
        "variantsHistory",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .name) {
        self.name = value
      }
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .displayName) {
        self.displayName = value
      }
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .description) {
        self.description = value
      }
      if let value = try container.decodeIfPresent(Experiment.State.self, forKey: .state) {
        self.state = value
      }
      self.definition = try container.decodeIfPresent(
        Experiment.Definition.self, forKey: .definition)
      self.rolloutConfig = try container.decodeIfPresent(RolloutConfig.self, forKey: .rolloutConfig)
      self.rolloutState = try container.decodeIfPresent(RolloutState.self, forKey: .rolloutState)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .rolloutFailureReason)
      {
        self.rolloutFailureReason = value
      }
      self.result = try container.decodeIfPresent(Experiment.Result.self, forKey: .result)
      self.createTime = try container.decodeIfPresent(GoogleWKT.Timestamp.self, forKey: .createTime)
      self.startTime = try container.decodeIfPresent(GoogleWKT.Timestamp.self, forKey: .startTime)
      self.endTime = try container.decodeIfPresent(GoogleWKT.Timestamp.self, forKey: .endTime)
      self.lastUpdateTime = try container.decodeIfPresent(
        GoogleWKT.Timestamp.self, forKey: .lastUpdateTime)
      self.experimentLength = try container.decodeIfPresent(
        GoogleWKT.Duration.self, forKey: .experimentLength)
      if let value = try container.decodeIfPresent([VariantsHistory].self, forKey: .variantsHistory)
      {
        self.variantsHistory = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.name, forKey: .name)
      try container.encode(self.displayName, forKey: .displayName)
      try container.encode(self.description, forKey: .description)
      try container.encode(self.state, forKey: .state)
      try container.encodeIfPresent(self.definition, forKey: .definition)
      try container.encodeIfPresent(self.rolloutConfig, forKey: .rolloutConfig)
      try container.encodeIfPresent(self.rolloutState, forKey: .rolloutState)
      try container.encode(self.rolloutFailureReason, forKey: .rolloutFailureReason)
      try container.encodeIfPresent(self.result, forKey: .result)
      try container.encodeIfPresent(self.createTime, forKey: .createTime)
      try container.encodeIfPresent(self.startTime, forKey: .startTime)
      try container.encodeIfPresent(self.endTime, forKey: .endTime)
      try container.encodeIfPresent(self.lastUpdateTime, forKey: .lastUpdateTime)
      try container.encodeIfPresent(self.experimentLength, forKey: .experimentLength)
      try container.encode(self.variantsHistory, forKey: .variantsHistory)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    /// Definition of the experiment.
    public struct Definition: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// The condition defines which subset of sessions are selected for
      /// this experiment. If not specified, all sessions are eligible. E.g.
      /// "query_input.language_code=en" See the [conditions
      /// reference](https://cloud.google.com/dialogflow/cx/docs/reference/condition).
      public var condition: Swift.String = Swift.String()

      /// The variants of the experiment. We currently only support single variant
      /// experiment.
      public var variants: OneOf_Variants? = nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `Definition`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Definition().with { $0.condition = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let condition = CodingKeys(stringValue: "condition")
        static let versionVariants = CodingKeys(stringValue: "versionVariants")

        static let _knownKeys: Set<Swift.String> = [
          "condition",
          "versionVariants",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .condition) {
          self.condition = value
        }

        var variants: OneOf_Variants? = nil
        let variantsCheckAndSet = {
          if variants != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'variants'"))
          }
          variants = $0
        }
        if let versionVariants = try container.decodeIfPresent(
          VersionVariants?.self, forKey: .versionVariants)
        {
          try variantsCheckAndSet(.versionVariants(versionVariants))
        }
        self.variants = variants
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.condition, forKey: .condition)

        if let choice = self.variants {
          switch choice {
          case .versionVariants(let value):
            try container.encode(value, forKey: .versionVariants)
          }
        }
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      /// The variants of the experiment. We currently only support single variant
      /// experiment.
      public enum OneOf_Variants: Codable, Equatable, Sendable {
        /// The flow versions as the variants of this experiment.
        indirect case versionVariants(VersionVariants?)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Experiment.Definition"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// The inference result which includes an objective metric to optimize and the
    /// confidence interval.
    public struct Result: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Version variants and metrics.
      public var versionMetrics: [Experiment.Result.VersionMetrics] = []

      /// The last time the experiment's stats data was updated. Will have default
      /// value if stats have never been computed for this experiment.
      public var lastUpdateTime: GoogleWKT.Timestamp? = nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `Result`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Result().with { $0.versionMetrics = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let versionMetrics = CodingKeys(stringValue: "versionMetrics")
        static let lastUpdateTime = CodingKeys(stringValue: "lastUpdateTime")

        static let _knownKeys: Set<Swift.String> = [
          "versionMetrics",
          "lastUpdateTime",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(
          [Experiment.Result.VersionMetrics].self, forKey: .versionMetrics)
        {
          self.versionMetrics = value
        }
        self.lastUpdateTime = try container.decodeIfPresent(
          GoogleWKT.Timestamp.self, forKey: .lastUpdateTime)
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.versionMetrics, forKey: .versionMetrics)
        try container.encodeIfPresent(self.lastUpdateTime, forKey: .lastUpdateTime)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      /// A confidence interval is a range of possible values for the experiment
      /// objective you are trying to measure.
      public struct ConfidenceInterval: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// The confidence level used to construct the interval, i.e. there is X%
        /// chance that the true value is within this interval.
        public var confidenceLevel: Swift.Double = Swift.Double()

        /// The percent change between an experiment metric's value and the value
        /// for its control.
        public var ratio: Swift.Double = Swift.Double()

        /// Lower bound of the interval.
        public var lowerBound: Swift.Double = Swift.Double()

        /// Upper bound of the interval.
        public var upperBound: Swift.Double = Swift.Double()

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `ConfidenceInterval`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = ConfidenceInterval().with { $0.confidenceLevel = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let confidenceLevel = CodingKeys(stringValue: "confidenceLevel")
          static let ratio = CodingKeys(stringValue: "ratio")
          static let lowerBound = CodingKeys(stringValue: "lowerBound")
          static let upperBound = CodingKeys(stringValue: "upperBound")

          static let _knownKeys: Set<Swift.String> = [
            "confidenceLevel",
            "ratio",
            "lowerBound",
            "upperBound",
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          if let value = try container.decodeIfPresent(Swift.Double.self, forKey: .confidenceLevel)
          {
            self.confidenceLevel = value
          }
          if let value = try container.decodeIfPresent(Swift.Double.self, forKey: .ratio) {
            self.ratio = value
          }
          if let value = try container.decodeIfPresent(Swift.Double.self, forKey: .lowerBound) {
            self.lowerBound = value
          }
          if let value = try container.decodeIfPresent(Swift.Double.self, forKey: .upperBound) {
            self.upperBound = value
          }
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.confidenceLevel, forKey: .confidenceLevel)
          try container.encode(self.ratio, forKey: .ratio)
          try container.encode(self.lowerBound, forKey: .lowerBound)
          try container.encode(self.upperBound, forKey: .upperBound)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.dialogflow.cx.v3.Experiment.Result.ConfidenceInterval"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      /// Metric and corresponding confidence intervals.
      public struct Metric: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// Ratio-based metric type. Only one of type or count_type is specified in
        /// each Metric.
        public var type: Experiment.Result.MetricType = Experiment.Result.MetricType()

        /// Count-based metric type. Only one of type or count_type is specified in
        /// each Metric.
        public var countType: Experiment.Result.CountType = Experiment.Result.CountType()

        /// The probability that the treatment is better than all other treatments
        /// in the experiment
        public var confidenceInterval: Experiment.Result.ConfidenceInterval? = nil

        /// The actual value of the metric.
        public var value: OneOf_Value? = nil

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `Metric`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Metric().with { $0.type = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let type = CodingKeys(stringValue: "type")
          static let countType = CodingKeys(stringValue: "countType")
          static let ratio = CodingKeys(stringValue: "ratio")
          static let count = CodingKeys(stringValue: "count")
          static let confidenceInterval = CodingKeys(stringValue: "confidenceInterval")

          static let _knownKeys: Set<Swift.String> = [
            "type",
            "countType",
            "ratio",
            "count",
            "confidenceInterval",
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          if let value = try container.decodeIfPresent(
            Experiment.Result.MetricType.self, forKey: .type)
          {
            self.type = value
          }
          if let value = try container.decodeIfPresent(
            Experiment.Result.CountType.self, forKey: .countType)
          {
            self.countType = value
          }
          self.confidenceInterval = try container.decodeIfPresent(
            Experiment.Result.ConfidenceInterval.self, forKey: .confidenceInterval)

          var value: OneOf_Value? = nil
          let valueCheckAndSet = {
            if value != nil {
              throw DecodingError.dataCorrupted(
                DecodingError.Context(
                  codingPath: decoder.codingPath,
                  debugDescription: "Multiple values set for oneof 'value'"))
            }
            value = $0
          }
          if let ratio = try container.decodeIfPresent(Swift.Double.self, forKey: .ratio) {
            try valueCheckAndSet(.ratio(ratio))
          }
          if let count = try container.decodeIfPresent(Swift.Double.self, forKey: .count) {
            try valueCheckAndSet(.count(count))
          }
          self.value = value
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.type, forKey: .type)
          try container.encode(self.countType, forKey: .countType)
          try container.encodeIfPresent(self.confidenceInterval, forKey: .confidenceInterval)

          if let choice = self.value {
            switch choice {
            case .ratio(let value):
              try container.encode(value, forKey: .ratio)
            case .count(let value):
              try container.encode(value, forKey: .count)
            }
          }
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        /// The actual value of the metric.
        public enum OneOf_Value: Codable, Equatable, Sendable {
          /// Ratio value of a metric.
          case ratio(Swift.Double)
          /// Count value of a metric.
          case count(Swift.Double)
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Experiment.Result.Metric"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      /// Version variant and associated metrics.
      public struct VersionMetrics: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// The name of the flow [Version][google.cloud.dialogflow.cx.v3.Version].
        /// Format:
        /// `projects/<ProjectID>/locations/<LocationID>/agents/<AgentID>/flows/<FlowID>/versions/<VersionID>`.
        ///
        /// [google.cloud.dialogflow.cx.v3.Version]: <doc:Version>
        public var version: Swift.String = Swift.String()

        /// The metrics and corresponding confidence intervals in the inference
        /// result.
        public var metrics: [Experiment.Result.Metric] = []

        /// Number of sessions that were allocated to this version.
        public var sessionCount: Swift.Int32 = Swift.Int32()

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `VersionMetrics`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = VersionMetrics().with { $0.version = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let version = CodingKeys(stringValue: "version")
          static let metrics = CodingKeys(stringValue: "metrics")
          static let sessionCount = CodingKeys(stringValue: "sessionCount")

          static let _knownKeys: Set<Swift.String> = [
            "version",
            "metrics",
            "sessionCount",
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          if let value = try container.decodeIfPresent(Swift.String.self, forKey: .version) {
            self.version = value
          }
          if let value = try container.decodeIfPresent(
            [Experiment.Result.Metric].self, forKey: .metrics)
          {
            self.metrics = value
          }
          if let value = try container.decodeIfPresent(Swift.Int32.self, forKey: .sessionCount) {
            self.sessionCount = value
          }
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.version, forKey: .version)
          try container.encode(self.metrics, forKey: .metrics)
          try container.encode(self.sessionCount, forKey: .sessionCount)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.dialogflow.cx.v3.Experiment.Result.VersionMetrics"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      /// Types of ratio-based metric for Dialogflow experiment.
      ///
      /// - Note: Adding cases to this enumeration is not considered a breaking change.
      ///   Always include an `@unknown default:` case when switching over this type.
      ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
      ///   expecting specific values to remain unparsed; future releases may promote
      ///   them to named cases.
      public enum MetricType: Codable, Equatable, Sendable {
        /// Metric unspecified.
        case metricUnspecified
        /// Percentage of contained sessions without user calling back in 24 hours.
        case containedSessionNoCallbackRate
        /// Percentage of sessions that were handed to a human agent.
        case liveAgentHandoffRate
        /// Percentage of sessions with the same user calling back.
        case callbackSessionRate
        /// Percentage of sessions where user hung up.
        case abandonedSessionRate
        /// Percentage of sessions reached Dialogflow 'END_PAGE' or
        /// 'END_SESSION'.
        case sessionEndRate
        /// Encodes an unknown integer value.
        ///
        /// The most common cause for an unknown value is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        ///
        /// - Warning: Do not pattern-match specific integer values in this case;
        ///   future releases may promote them to named enum cases.
        case unknownIntValue(Int)
        /// Encodes an unknown string value.
        ///
        /// The most common cause for an unknown value is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        ///
        /// - Warning: Do not pattern-match specific string literals in this case;
        ///   future releases may promote them to named enum cases.
        case unknownStringValue(String)

        public init() {
          self = .metricUnspecified
        }

        /// Returns the integer value associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown string value, this returns `nil`.
        public var intValue: Int? {
          switch self {
          case .metricUnspecified: return 0
          case .containedSessionNoCallbackRate: return 1
          case .liveAgentHandoffRate: return 2
          case .callbackSessionRate: return 3
          case .abandonedSessionRate: return 4
          case .sessionEndRate: return 5
          case .unknownIntValue(let v): return v
          case .unknownStringValue: return nil
          }
        }

        /// Returns the string value (or name) associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
        public var stringValue: Swift.String? {
          switch self {
          case .metricUnspecified: return "METRIC_UNSPECIFIED"
          case .containedSessionNoCallbackRate: return "CONTAINED_SESSION_NO_CALLBACK_RATE"
          case .liveAgentHandoffRate: return "LIVE_AGENT_HANDOFF_RATE"
          case .callbackSessionRate: return "CALLBACK_SESSION_RATE"
          case .abandonedSessionRate: return "ABANDONED_SESSION_RATE"
          case .sessionEndRate: return "SESSION_END_RATE"
          case .unknownIntValue: return nil
          case .unknownStringValue(let v): return v
          }
        }

        /// Initialize from a string value.
        ///
        /// If the value is unknown, this initializes to [`unknownStringValue`](doc:MetricType/unknownStringValue(_:)).
        public init(stringValue: Swift.String) {
          switch stringValue {
          case "METRIC_UNSPECIFIED": self = .metricUnspecified
          case "CONTAINED_SESSION_NO_CALLBACK_RATE": self = .containedSessionNoCallbackRate
          case "LIVE_AGENT_HANDOFF_RATE": self = .liveAgentHandoffRate
          case "CALLBACK_SESSION_RATE": self = .callbackSessionRate
          case "ABANDONED_SESSION_RATE": self = .abandonedSessionRate
          case "SESSION_END_RATE": self = .sessionEndRate
          default: self = .unknownStringValue(stringValue)
          }
        }

        /// Initialize from an integer value.
        ///
        /// If the value is unknown, this initializes to [`unknownIntValue`](doc:MetricType/unknownIntValue(_:)).
        public init(intValue: Int) {
          switch intValue {
          case 0: self = .metricUnspecified
          case 1: self = .containedSessionNoCallbackRate
          case 2: self = .liveAgentHandoffRate
          case 3: self = .callbackSessionRate
          case 4: self = .abandonedSessionRate
          case 5: self = .sessionEndRate
          default: self = .unknownIntValue(intValue)
          }
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.singleValueContainer()
          if let v = try? container.decode(Int.self) {
            self.init(intValue: v)
            return
          }
          if let s = try? container.decode(String.self) {
            if let v = Int(s) {
              self.init(intValue: v)
            } else {
              self.init(stringValue: s)
            }
            return
          }
          throw DecodingError.dataCorruptedError(
            in: container, debugDescription: "Expected enum value, must be integer or string.")
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.singleValueContainer()
          switch self {
          case .metricUnspecified: return try container.encode("METRIC_UNSPECIFIED")
          case .containedSessionNoCallbackRate:
            return try container.encode("CONTAINED_SESSION_NO_CALLBACK_RATE")
          case .liveAgentHandoffRate: return try container.encode("LIVE_AGENT_HANDOFF_RATE")
          case .callbackSessionRate: return try container.encode("CALLBACK_SESSION_RATE")
          case .abandonedSessionRate: return try container.encode("ABANDONED_SESSION_RATE")
          case .sessionEndRate: return try container.encode("SESSION_END_RATE")
          case .unknownIntValue(let v): return try container.encode(v)
          case .unknownStringValue(let v): return try container.encode(v)
          }
        }
      }

      /// Types of count-based metric for Dialogflow experiment.
      ///
      /// - Note: Adding cases to this enumeration is not considered a breaking change.
      ///   Always include an `@unknown default:` case when switching over this type.
      ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
      ///   expecting specific values to remain unparsed; future releases may promote
      ///   them to named cases.
      public enum CountType: Codable, Equatable, Sendable {
        /// Count type unspecified.
        case unspecified
        /// Total number of occurrences of a 'NO_MATCH'.
        case totalNoMatchCount
        /// Total number of turn counts.
        case totalTurnCount
        /// Average turn count in a session.
        case averageTurnCount
        /// Encodes an unknown integer value.
        ///
        /// The most common cause for an unknown value is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        ///
        /// - Warning: Do not pattern-match specific integer values in this case;
        ///   future releases may promote them to named enum cases.
        case unknownIntValue(Int)
        /// Encodes an unknown string value.
        ///
        /// The most common cause for an unknown value is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        ///
        /// - Warning: Do not pattern-match specific string literals in this case;
        ///   future releases may promote them to named enum cases.
        case unknownStringValue(String)

        public init() {
          self = .unspecified
        }

        /// Returns the integer value associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown string value, this returns `nil`.
        public var intValue: Int? {
          switch self {
          case .unspecified: return 0
          case .totalNoMatchCount: return 1
          case .totalTurnCount: return 2
          case .averageTurnCount: return 3
          case .unknownIntValue(let v): return v
          case .unknownStringValue: return nil
          }
        }

        /// Returns the string value (or name) associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
        public var stringValue: Swift.String? {
          switch self {
          case .unspecified: return "COUNT_TYPE_UNSPECIFIED"
          case .totalNoMatchCount: return "TOTAL_NO_MATCH_COUNT"
          case .totalTurnCount: return "TOTAL_TURN_COUNT"
          case .averageTurnCount: return "AVERAGE_TURN_COUNT"
          case .unknownIntValue: return nil
          case .unknownStringValue(let v): return v
          }
        }

        /// Initialize from a string value.
        ///
        /// If the value is unknown, this initializes to [`unknownStringValue`](doc:CountType/unknownStringValue(_:)).
        public init(stringValue: Swift.String) {
          switch stringValue {
          case "COUNT_TYPE_UNSPECIFIED": self = .unspecified
          case "TOTAL_NO_MATCH_COUNT": self = .totalNoMatchCount
          case "TOTAL_TURN_COUNT": self = .totalTurnCount
          case "AVERAGE_TURN_COUNT": self = .averageTurnCount
          default: self = .unknownStringValue(stringValue)
          }
        }

        /// Initialize from an integer value.
        ///
        /// If the value is unknown, this initializes to [`unknownIntValue`](doc:CountType/unknownIntValue(_:)).
        public init(intValue: Int) {
          switch intValue {
          case 0: self = .unspecified
          case 1: self = .totalNoMatchCount
          case 2: self = .totalTurnCount
          case 3: self = .averageTurnCount
          default: self = .unknownIntValue(intValue)
          }
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.singleValueContainer()
          if let v = try? container.decode(Int.self) {
            self.init(intValue: v)
            return
          }
          if let s = try? container.decode(String.self) {
            if let v = Int(s) {
              self.init(intValue: v)
            } else {
              self.init(stringValue: s)
            }
            return
          }
          throw DecodingError.dataCorruptedError(
            in: container, debugDescription: "Expected enum value, must be integer or string.")
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.singleValueContainer()
          switch self {
          case .unspecified: return try container.encode("COUNT_TYPE_UNSPECIFIED")
          case .totalNoMatchCount: return try container.encode("TOTAL_NO_MATCH_COUNT")
          case .totalTurnCount: return try container.encode("TOTAL_TURN_COUNT")
          case .averageTurnCount: return try container.encode("AVERAGE_TURN_COUNT")
          case .unknownIntValue(let v): return try container.encode(v)
          case .unknownStringValue(let v): return try container.encode(v)
          }
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Experiment.Result"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// The state of the experiment.
    ///
    /// - Note: Adding cases to this enumeration is not considered a breaking change.
    ///   Always include an `@unknown default:` case when switching over this type.
    ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
    ///   expecting specific values to remain unparsed; future releases may promote
    ///   them to named cases.
    public enum State: Codable, Equatable, Sendable {
      /// State unspecified.
      case unspecified
      /// The experiment is created but not started yet.
      case draft
      /// The experiment is running.
      case running
      /// The experiment is done.
      case done
      /// The experiment with auto-rollout enabled has failed.
      case rolloutFailed
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown value is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      ///
      /// - Warning: Do not pattern-match specific integer values in this case;
      ///   future releases may promote them to named enum cases.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown value is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      ///
      /// - Warning: Do not pattern-match specific string literals in this case;
      ///   future releases may promote them to named enum cases.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .draft: return 1
        case .running: return 2
        case .done: return 3
        case .rolloutFailed: return 4
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "STATE_UNSPECIFIED"
        case .draft: return "DRAFT"
        case .running: return "RUNNING"
        case .done: return "DONE"
        case .rolloutFailed: return "ROLLOUT_FAILED"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:State/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "STATE_UNSPECIFIED": self = .unspecified
        case "DRAFT": self = .draft
        case "RUNNING": self = .running
        case "DONE": self = .done
        case "ROLLOUT_FAILED": self = .rolloutFailed
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:State/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .draft
        case 2: self = .running
        case 3: self = .done
        case 4: self = .rolloutFailed
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode("STATE_UNSPECIFIED")
        case .draft: return try container.encode("DRAFT")
        case .running: return try container.encode("RUNNING")
        case .done: return try container.encode("DONE")
        case .rolloutFailed: return try container.encode("ROLLOUT_FAILED")
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Experiment"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }
#endif
