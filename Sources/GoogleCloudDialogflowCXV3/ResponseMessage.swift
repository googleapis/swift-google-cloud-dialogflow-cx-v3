// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if Flows || Pages || Playbooks || Sessions || TestCases || TransitionRouteGroups
  import Foundation
  import GoogleCloudWkt

  /// Represents a response message that can be returned by a conversational agent.
  ///
  /// Response messages are also used for output audio synthesis. The approach is
  /// as follows:
  ///
  /// * If at least one OutputAudioText response is present, then all
  ///   OutputAudioText responses are linearly concatenated, and the result is used
  ///   for output audio synthesis.
  /// * If the OutputAudioText responses are a mixture of text and SSML, then the
  ///   concatenated result is treated as SSML; otherwise, the result is treated as
  ///   either text or SSML as appropriate. The agent designer should ideally use
  ///   either text or SSML consistently throughout the bot design.
  /// * Otherwise, all Text responses are linearly concatenated, and the result is
  ///   used for output audio synthesis.
  ///
  /// This approach allows for more sophisticated user experience scenarios, where
  /// the text displayed to the user may differ from what is heard.
  public struct ResponseMessage: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Response type.
    public var responseType: ResponseMessage.ResponseType = ResponseMessage.ResponseType()

    /// The channel which the response is associated with. Clients can specify the
    /// channel via
    /// [QueryParameters.channel][google.cloud.dialogflow.cx.v3.QueryParameters.channel],
    /// and only associated channel response will be returned.
    ///
    /// [google.cloud.dialogflow.cx.v3.QueryParameters.channel]: <doc:QueryParameters/channel>
    public var channel: Swift.String = Swift.String()

    /// Required. The rich response message.
    public var message: OneOf_Message? = nil

    /// Initialize a new instance of `ResponseMessage`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ResponseMessage().with { $0.text = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case text = "text"
      case payload = "payload"
      case conversationSuccess = "conversationSuccess"
      case outputAudioText = "outputAudioText"
      case liveAgentHandoff = "liveAgentHandoff"
      case endInteraction = "endInteraction"
      case playAudio = "playAudio"
      case mixedAudio = "mixedAudio"
      case telephonyTransferCall = "telephonyTransferCall"
      case knowledgeInfoCard = "knowledgeInfoCard"
      case toolCall = "toolCall"
      case responseType = "responseType"
      case channel = "channel"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.responseType = try container.decode(
        ResponseMessage.ResponseType.self, forKey: .responseType)
      self.channel = try container.decode(Swift.String.self, forKey: .channel)

      var message: OneOf_Message? = nil
      let messageCheckAndSet = {
        if message != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'message'"))
        }
        message = $0
      }
      if let text = try container.decodeIfPresent(ResponseMessage.Text?.self, forKey: .text) {
        try messageCheckAndSet(.text(text))
      }
      if let payload = try container.decodeIfPresent(GoogleCloudWkt.Struct?.self, forKey: .payload)
      {
        try messageCheckAndSet(.payload(payload))
      }
      if let conversationSuccess = try container.decodeIfPresent(
        ResponseMessage.ConversationSuccess?.self, forKey: .conversationSuccess)
      {
        try messageCheckAndSet(.conversationSuccess(conversationSuccess))
      }
      if let outputAudioText = try container.decodeIfPresent(
        ResponseMessage.OutputAudioText?.self, forKey: .outputAudioText)
      {
        try messageCheckAndSet(.outputAudioText(outputAudioText))
      }
      if let liveAgentHandoff = try container.decodeIfPresent(
        ResponseMessage.LiveAgentHandoff?.self, forKey: .liveAgentHandoff)
      {
        try messageCheckAndSet(.liveAgentHandoff(liveAgentHandoff))
      }
      if let endInteraction = try container.decodeIfPresent(
        ResponseMessage.EndInteraction?.self, forKey: .endInteraction)
      {
        try messageCheckAndSet(.endInteraction(endInteraction))
      }
      if let playAudio = try container.decodeIfPresent(
        ResponseMessage.PlayAudio?.self, forKey: .playAudio)
      {
        try messageCheckAndSet(.playAudio(playAudio))
      }
      if let mixedAudio = try container.decodeIfPresent(
        ResponseMessage.MixedAudio?.self, forKey: .mixedAudio)
      {
        try messageCheckAndSet(.mixedAudio(mixedAudio))
      }
      if let telephonyTransferCall = try container.decodeIfPresent(
        ResponseMessage.TelephonyTransferCall?.self, forKey: .telephonyTransferCall)
      {
        try messageCheckAndSet(.telephonyTransferCall(telephonyTransferCall))
      }
      if let knowledgeInfoCard = try container.decodeIfPresent(
        ResponseMessage.KnowledgeInfoCard?.self, forKey: .knowledgeInfoCard)
      {
        try messageCheckAndSet(.knowledgeInfoCard(knowledgeInfoCard))
      }
      if let toolCall = try container.decodeIfPresent(ToolCall?.self, forKey: .toolCall) {
        try messageCheckAndSet(.toolCall(toolCall))
      }
      self.message = message
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.responseType, forKey: .responseType)
      try container.encode(self.channel, forKey: .channel)

      if let choice = self.message {
        switch choice {
        case .text(let value):
          try container.encode(value, forKey: .text)
        case .payload(let value):
          try container.encode(value, forKey: .payload)
        case .conversationSuccess(let value):
          try container.encode(value, forKey: .conversationSuccess)
        case .outputAudioText(let value):
          try container.encode(value, forKey: .outputAudioText)
        case .liveAgentHandoff(let value):
          try container.encode(value, forKey: .liveAgentHandoff)
        case .endInteraction(let value):
          try container.encode(value, forKey: .endInteraction)
        case .playAudio(let value):
          try container.encode(value, forKey: .playAudio)
        case .mixedAudio(let value):
          try container.encode(value, forKey: .mixedAudio)
        case .telephonyTransferCall(let value):
          try container.encode(value, forKey: .telephonyTransferCall)
        case .knowledgeInfoCard(let value):
          try container.encode(value, forKey: .knowledgeInfoCard)
        case .toolCall(let value):
          try container.encode(value, forKey: .toolCall)
        }
      }
    }

    /// The text response message.
    public struct Text: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Required. A collection of text response variants. If multiple variants
      /// are defined, only one text response variant is returned at runtime.
      public var text: [Swift.String] = []

      /// Output only. Whether the playback of this message can be interrupted by
      /// the end user's speech and the client can then starts the next Dialogflow
      /// request.
      public var allowPlaybackInterruption: Swift.Bool = Swift.Bool()

      /// Initialize a new instance of `Text`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Text().with { $0.text = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.ResponseMessage.Text"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Indicates that the conversation should be handed off to a live agent.
    ///
    /// Dialogflow only uses this to determine which conversations were handed off
    /// to a human agent for measurement purposes. What else to do with this signal
    /// is up to you and your handoff procedures.
    ///
    /// You may set this, for example:
    /// * In the
    /// [entry_fulfillment][google.cloud.dialogflow.cx.v3.Page.entry_fulfillment]
    /// of a [Page][google.cloud.dialogflow.cx.v3.Page] if
    ///   entering the page indicates something went extremely wrong in the
    ///   conversation.
    /// * In a webhook response when you determine that the customer issue can only
    ///   be handled by a human.
    ///
    /// [google.cloud.dialogflow.cx.v3.Page]: <doc:Page>
    /// [google.cloud.dialogflow.cx.v3.Page.entry_fulfillment]: <doc:Page/entryFulfillment>
    public struct LiveAgentHandoff: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Custom metadata for your handoff procedure. Dialogflow doesn't impose
      /// any structure on this.
      public var metadata: GoogleCloudWkt.Struct? = nil

      /// Initialize a new instance of `LiveAgentHandoff`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = LiveAgentHandoff().with { $0.metadata = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.ResponseMessage.LiveAgentHandoff"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Indicates that the conversation succeeded, i.e., the bot handled the issue
    /// that the customer talked to it about.
    ///
    /// Dialogflow only uses this to determine which conversations should be
    /// counted as successful and doesn't process the metadata in this message in
    /// any way. Note that Dialogflow also considers conversations that get to the
    /// conversation end page as successful even if they don't return
    /// [ConversationSuccess][google.cloud.dialogflow.cx.v3.ResponseMessage.ConversationSuccess].
    ///
    /// You may set this, for example:
    /// * In the
    /// [entry_fulfillment][google.cloud.dialogflow.cx.v3.Page.entry_fulfillment]
    /// of a [Page][google.cloud.dialogflow.cx.v3.Page] if
    ///   entering the page indicates that the conversation succeeded.
    /// * In a webhook response when you determine that you handled the customer
    ///   issue.
    ///
    /// [google.cloud.dialogflow.cx.v3.Page]: <doc:Page>
    /// [google.cloud.dialogflow.cx.v3.Page.entry_fulfillment]: <doc:Page/entryFulfillment>
    /// [google.cloud.dialogflow.cx.v3.ResponseMessage.ConversationSuccess]: <doc:ResponseMessage/ConversationSuccess>
    public struct ConversationSuccess: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Custom metadata. Dialogflow doesn't impose any structure on this.
      public var metadata: GoogleCloudWkt.Struct? = nil

      /// Initialize a new instance of `ConversationSuccess`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = ConversationSuccess().with { $0.metadata = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.dialogflow.cx.v3.ResponseMessage.ConversationSuccess"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// A text or ssml response that is preferentially used for TTS output audio
    /// synthesis, as described in the comment on the ResponseMessage message.
    public struct OutputAudioText: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Output only. Whether the playback of this message can be interrupted by
      /// the end user's speech and the client can then starts the next Dialogflow
      /// request.
      public var allowPlaybackInterruption: Swift.Bool = Swift.Bool()

      /// The source, which is either plain text or SSML.
      public var source: OneOf_Source? = nil

      /// Initialize a new instance of `OutputAudioText`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = OutputAudioText().with { $0.text = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case text = "text"
        case ssml = "ssml"
        case allowPlaybackInterruption = "allowPlaybackInterruption"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.allowPlaybackInterruption = try container.decode(
          Swift.Bool.self, forKey: .allowPlaybackInterruption)

        var source: OneOf_Source? = nil
        let sourceCheckAndSet = {
          if source != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'source'"))
          }
          source = $0
        }
        if let text = try container.decodeIfPresent(Swift.String.self, forKey: .text) {
          try sourceCheckAndSet(.text(text))
        }
        if let ssml = try container.decodeIfPresent(Swift.String.self, forKey: .ssml) {
          try sourceCheckAndSet(.ssml(ssml))
        }
        self.source = source
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.allowPlaybackInterruption, forKey: .allowPlaybackInterruption)

        if let choice = self.source {
          switch choice {
          case .text(let value):
            try container.encode(value, forKey: .text)
          case .ssml(let value):
            try container.encode(value, forKey: .ssml)
          }
        }
      }

      /// The source, which is either plain text or SSML.
      public enum OneOf_Source: Codable, Equatable, Sendable {
        /// The raw text to be synthesized.
        case text(Swift.String)
        /// The SSML text to be synthesized. For more information, see
        /// [SSML](/speech/text-to-speech/docs/ssml).
        case ssml(Swift.String)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.ResponseMessage.OutputAudioText"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Indicates that interaction with the Dialogflow agent has ended.
    /// This message is generated by Dialogflow only and not supposed to be
    /// defined by the user.
    public struct EndInteraction: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Initialize a new instance of `EndInteraction`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = EndInteraction().with { $0.<placeholder> = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.ResponseMessage.EndInteraction"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Specifies an audio clip to be played by the client as part of the response.
    public struct PlayAudio: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Required. URI of the audio clip. Dialogflow does not impose any
      /// validation on this value. It is specific to the client that reads it.
      public var audioUri: Swift.String = Swift.String()

      /// Output only. Whether the playback of this message can be interrupted by
      /// the end user's speech and the client can then starts the next Dialogflow
      /// request.
      public var allowPlaybackInterruption: Swift.Bool = Swift.Bool()

      /// Initialize a new instance of `PlayAudio`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = PlayAudio().with { $0.audioUri = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.ResponseMessage.PlayAudio"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Represents an audio message that is composed of both segments
    /// synthesized from the Dialogflow agent prompts and ones hosted externally
    /// at the specified URIs.
    /// The external URIs are specified via
    /// [play_audio][google.cloud.dialogflow.cx.v3.ResponseMessage.play_audio].
    /// This message is generated by Dialogflow only and not supposed to be
    /// defined by the user.
    ///
    /// [google.cloud.dialogflow.cx.v3.ResponseMessage.play_audio]: <doc:ResponseMessage/playAudio>
    public struct MixedAudio: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Segments this audio response is composed of.
      public var segments: [ResponseMessage.MixedAudio.Segment] = []

      /// Initialize a new instance of `MixedAudio`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = MixedAudio().with { $0.segments = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// Represents one segment of audio.
      public struct Segment: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Output only. Whether the playback of this segment can be interrupted by
        /// the end user's speech and the client should then start the next
        /// Dialogflow request.
        public var allowPlaybackInterruption: Swift.Bool = Swift.Bool()

        /// Content of the segment.
        public var content: OneOf_Content? = nil

        /// Initialize a new instance of `Segment`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Segment().with { $0.audio = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private enum CodingKeys: Swift.String, CodingKey {
          case audio = "audio"
          case uri = "uri"
          case allowPlaybackInterruption = "allowPlaybackInterruption"
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          self.allowPlaybackInterruption = try container.decode(
            Swift.Bool.self, forKey: .allowPlaybackInterruption)

          var content: OneOf_Content? = nil
          let contentCheckAndSet = {
            if content != nil {
              throw DecodingError.dataCorrupted(
                DecodingError.Context(
                  codingPath: decoder.codingPath,
                  debugDescription: "Multiple values set for oneof 'content'"))
            }
            content = $0
          }
          if let audio = try container.decodeIfPresent(Foundation.Data.self, forKey: .audio) {
            try contentCheckAndSet(.audio(audio))
          }
          if let uri = try container.decodeIfPresent(Swift.String.self, forKey: .uri) {
            try contentCheckAndSet(.uri(uri))
          }
          self.content = content
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.allowPlaybackInterruption, forKey: .allowPlaybackInterruption)

          if let choice = self.content {
            switch choice {
            case .audio(let value):
              try container.encode(value, forKey: .audio)
            case .uri(let value):
              try container.encode(value, forKey: .uri)
            }
          }
        }

        /// Content of the segment.
        public enum OneOf_Content: Codable, Equatable, Sendable {
          /// Raw audio synthesized from the Dialogflow agent's response using
          /// the output config specified in the request.
          case audio(Foundation.Data)
          /// Client-specific URI that points to an audio clip accessible to the
          /// client. Dialogflow does not impose any validation on it.
          case uri(Swift.String)
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.dialogflow.cx.v3.ResponseMessage.MixedAudio.Segment"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.ResponseMessage.MixedAudio"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Represents the signal that telles the client to transfer the phone call
    /// connected to the agent to a third-party endpoint.
    public struct TelephonyTransferCall: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Endpoint to transfer the call to.
      public var endpoint: OneOf_Endpoint? = nil

      /// Initialize a new instance of `TelephonyTransferCall`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = TelephonyTransferCall().with { $0.phoneNumber = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case phoneNumber = "phoneNumber"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var endpoint: OneOf_Endpoint? = nil
        let endpointCheckAndSet = {
          if endpoint != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'endpoint'"))
          }
          endpoint = $0
        }
        if let phoneNumber = try container.decodeIfPresent(Swift.String.self, forKey: .phoneNumber)
        {
          try endpointCheckAndSet(.phoneNumber(phoneNumber))
        }
        self.endpoint = endpoint
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.endpoint {
          switch choice {
          case .phoneNumber(let value):
            try container.encode(value, forKey: .phoneNumber)
          }
        }
      }

      /// Endpoint to transfer the call to.
      public enum OneOf_Endpoint: Codable, Equatable, Sendable {
        /// Transfer the call to a phone number
        /// in [E.164 format](https://en.wikipedia.org/wiki/E.164).
        case phoneNumber(Swift.String)
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.dialogflow.cx.v3.ResponseMessage.TelephonyTransferCall"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Represents info card response. If the response contains generative
    /// knowledge prediction, Dialogflow will return a payload with Infobot
    /// Messenger compatible info card.
    ///
    /// Otherwise, the info card response is skipped.
    public struct KnowledgeInfoCard: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Initialize a new instance of `KnowledgeInfoCard`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = KnowledgeInfoCard().with { $0.<placeholder> = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.ResponseMessage.KnowledgeInfoCard"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Represents different response types.
    public enum ResponseType: Codable, Equatable, Sendable {
      /// Not specified.
      case unspecified
      /// The response is from an [entry
      /// prompt][google.cloud.dialogflow.cx.v3.Page.entry_fulfillment] in the
      /// page.
      ///
      /// [google.cloud.dialogflow.cx.v3.Page.entry_fulfillment]: <doc:Page/entryFulfillment>
      case entryPrompt
      /// The response is from [form-filling
      /// prompt][google.cloud.dialogflow.cx.v3.Form.Parameter.fill_behavior] in
      /// the page.
      ///
      /// [google.cloud.dialogflow.cx.v3.Form.Parameter.fill_behavior]: <doc:Form/Parameter/fillBehavior>
      case parameterPrompt
      /// The response is from a [transition
      /// route][google.cloud.dialogflow.cx.v3.TransitionRoute] or an [event
      /// handler][EventHandler] in the page or flow or transition route group.
      ///
      /// [EventHandler]: <doc:EventHandler>
      /// [google.cloud.dialogflow.cx.v3.TransitionRoute]: <doc:TransitionRoute>
      case handlerPrompt
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .entryPrompt: return 1
        case .parameterPrompt: return 2
        case .handlerPrompt: return 3
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "RESPONSE_TYPE_UNSPECIFIED"
        case .entryPrompt: return "ENTRY_PROMPT"
        case .parameterPrompt: return "PARAMETER_PROMPT"
        case .handlerPrompt: return "HANDLER_PROMPT"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ResponseType/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "RESPONSE_TYPE_UNSPECIFIED": self = .unspecified
        case "ENTRY_PROMPT": self = .entryPrompt
        case "PARAMETER_PROMPT": self = .parameterPrompt
        case "HANDLER_PROMPT": self = .handlerPrompt
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ResponseType/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .entryPrompt
        case 2: self = .parameterPrompt
        case 3: self = .handlerPrompt
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .entryPrompt: return try container.encode(1)
        case .parameterPrompt: return try container.encode(2)
        case .handlerPrompt: return try container.encode(3)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Required. The rich response message.
    public enum OneOf_Message: Codable, Equatable, Sendable {
      /// Returns a text response.
      indirect case text(ResponseMessage.Text?)
      /// Returns a response containing a custom, platform-specific payload.
      indirect case payload(GoogleCloudWkt.Struct?)
      /// Indicates that the conversation succeeded.
      indirect case conversationSuccess(ResponseMessage.ConversationSuccess?)
      /// A text or ssml response that is preferentially used for TTS output audio
      /// synthesis, as described in the comment on the ResponseMessage message.
      indirect case outputAudioText(ResponseMessage.OutputAudioText?)
      /// Hands off conversation to a human agent.
      indirect case liveAgentHandoff(ResponseMessage.LiveAgentHandoff?)
      /// Output only. A signal that indicates the interaction with the Dialogflow
      /// agent has ended. This message is generated by Dialogflow only when the
      /// conversation reaches `END_SESSION` page. It is not supposed to be defined
      /// by the user.
      ///
      /// It's guaranteed that there is at most one such message in each response.
      indirect case endInteraction(ResponseMessage.EndInteraction?)
      /// Signal that the client should play an audio clip hosted at a
      /// client-specific URI. Dialogflow uses this to construct
      /// [mixed_audio][google.cloud.dialogflow.cx.v3.ResponseMessage.mixed_audio].
      /// However, Dialogflow itself does not try to read or process the URI in any
      /// way.
      ///
      /// [google.cloud.dialogflow.cx.v3.ResponseMessage.mixed_audio]: <doc:ResponseMessage/mixedAudio>
      indirect case playAudio(ResponseMessage.PlayAudio?)
      /// Output only. An audio response message composed of both the synthesized
      /// Dialogflow agent responses and responses defined via
      /// [play_audio][google.cloud.dialogflow.cx.v3.ResponseMessage.play_audio].
      /// This message is generated by Dialogflow only and not supposed to be
      /// defined by the user.
      ///
      /// [google.cloud.dialogflow.cx.v3.ResponseMessage.play_audio]: <doc:ResponseMessage/playAudio>
      indirect case mixedAudio(ResponseMessage.MixedAudio?)
      /// A signal that the client should transfer the phone call connected to
      /// this agent to a third-party endpoint.
      indirect case telephonyTransferCall(ResponseMessage.TelephonyTransferCall?)
      /// Represents info card for knowledge answers, to be better rendered in
      /// Dialogflow Messenger.
      indirect case knowledgeInfoCard(ResponseMessage.KnowledgeInfoCard?)
      /// Returns the definition of a tool call that should be executed by the
      /// client.
      indirect case toolCall(ToolCall?)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dialogflow.cx.v3.ResponseMessage"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
