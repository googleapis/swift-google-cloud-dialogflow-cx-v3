// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if Agents
  import Foundation
  import GoogleCloudWkt

  /// Settings for Generative Safety.
  public struct SafetySettings: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. Default phrase match strategy for banned phrases.
    public var defaultBannedPhraseMatchStrategy: SafetySettings.PhraseMatchStrategy =
      SafetySettings.PhraseMatchStrategy()

    /// Banned phrases for generated text.
    public var bannedPhrases: [SafetySettings.Phrase] = []

    /// Optional. Settings for Responsible AI checks.
    public var raiSettings: SafetySettings.RaiSettings? = nil

    /// Optional. Immutable. Default RAI settings to be annotated on the agent, so
    /// that users will be able to restore their RAI configurations to the default
    /// settings. Read-only field for the API proto only.
    public var defaultRaiSettings: SafetySettings.RaiSettings? = nil

    /// Optional. Settings for prompt security checks.
    public var promptSecuritySettings: SafetySettings.PromptSecuritySettings? = nil

    /// Initialize a new instance of `SafetySettings`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = SafetySettings().with { $0.defaultBannedPhraseMatchStrategy = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Text input which can be used for prompt or banned phrases.
    public struct Phrase: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Required. Text input which can be used for prompt or banned phrases.
      public var text: Swift.String = Swift.String()

      /// Required. Language code of the phrase.
      public var languageCode: Swift.String = Swift.String()

      /// Initialize a new instance of `Phrase`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Phrase().with { $0.text = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.SafetySettings.Phrase"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Settings for Responsible AI.
    public struct RaiSettings: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Optional. RAI blocking configurations.
      public var categoryFilters: [SafetySettings.RaiSettings.CategoryFilter] = []

      /// Initialize a new instance of `RaiSettings`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = RaiSettings().with { $0.categoryFilters = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// Configuration of the sensitivity level for blocking an RAI category.
      public struct CategoryFilter: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// RAI category to configure.
        public var category: SafetySettings.RaiSettings.SafetyCategory = SafetySettings.RaiSettings
          .SafetyCategory()

        /// Blocking sensitivity level to configure for the RAI category.
        public var filterLevel: SafetySettings.RaiSettings.SafetyFilterLevel = SafetySettings
          .RaiSettings.SafetyFilterLevel()

        /// Initialize a new instance of `CategoryFilter`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = CategoryFilter().with { $0.category = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: String {
          return
            "type.googleapis.com/google.cloud.dialogflow.cx.v3.SafetySettings.RaiSettings.CategoryFilter"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Sensitivity level for RAI categories.
      public enum SafetyFilterLevel: Codable, Equatable, Sendable {
        /// Unspecified -- uses default sensitivity levels.
        case unspecified
        /// Block no text -- effectively disables the category.
        case blockNone
        /// Block a few suspicious texts.
        case blockFew
        /// Block some suspicious texts.
        case blockSome
        /// Block most suspicious texts.
        case blockMost
        /// Encodes an unknown integer value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownIntValue(Int)
        /// Encodes an unknown string value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownStringValue(String)

        public init() {
          self = .unspecified
        }

        /// Returns the integer value associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown string value, this returns `nil`.
        public var intValue: Int? {
          switch self {
          case .unspecified: return 0
          case .blockNone: return 1
          case .blockFew: return 2
          case .blockSome: return 3
          case .blockMost: return 4
          case .unknownIntValue(let v): return v
          case .unknownStringValue: return nil
          }
        }

        /// Returns the string value (or name) associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
        public var stringValue: String? {
          switch self {
          case .unspecified: return "SAFETY_FILTER_LEVEL_UNSPECIFIED"
          case .blockNone: return "BLOCK_NONE"
          case .blockFew: return "BLOCK_FEW"
          case .blockSome: return "BLOCK_SOME"
          case .blockMost: return "BLOCK_MOST"
          case .unknownIntValue: return nil
          case .unknownStringValue(let v): return v
          }
        }

        /// Initialize from a string value.
        ///
        /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
        public init(stringValue: String) {
          switch stringValue {
          case "SAFETY_FILTER_LEVEL_UNSPECIFIED": self = .unspecified
          case "BLOCK_NONE": self = .blockNone
          case "BLOCK_FEW": self = .blockFew
          case "BLOCK_SOME": self = .blockSome
          case "BLOCK_MOST": self = .blockMost
          default: self = .unknownStringValue(stringValue)
          }
        }

        /// Initialize from an integer value.
        ///
        /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
        public init(intValue: Int) {
          switch intValue {
          case 0: self = .unspecified
          case 1: self = .blockNone
          case 2: self = .blockFew
          case 3: self = .blockSome
          case 4: self = .blockMost
          default: self = .unknownIntValue(intValue)
          }
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.singleValueContainer()
          if let v = try? container.decode(Int.self) {
            self.init(intValue: v)
            return
          }
          if let s = try? container.decode(String.self) {
            if let v = Int(s) {
              self.init(intValue: v)
            } else {
              self.init(stringValue: s)
            }
            return
          }
          throw DecodingError.dataCorruptedError(
            in: container, debugDescription: "Expected enum value, must be integer or string.")
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.singleValueContainer()
          switch self {
          case .unspecified: return try container.encode(0)
          case .blockNone: return try container.encode(1)
          case .blockFew: return try container.encode(2)
          case .blockSome: return try container.encode(3)
          case .blockMost: return try container.encode(4)
          case .unknownIntValue(let v): return try container.encode(v)
          case .unknownStringValue(let v): return try container.encode(v)
          }
        }
      }

      /// RAI categories to configure.
      public enum SafetyCategory: Codable, Equatable, Sendable {
        /// Unspecified.
        case unspecified
        /// Dangerous content.
        case dangerousContent
        /// Hate speech.
        case hateSpeech
        /// Harassment.
        case harassment
        /// Sexually explicit content.
        case sexuallyExplicitContent
        /// Encodes an unknown integer value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownIntValue(Int)
        /// Encodes an unknown string value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownStringValue(String)

        public init() {
          self = .unspecified
        }

        /// Returns the integer value associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown string value, this returns `nil`.
        public var intValue: Int? {
          switch self {
          case .unspecified: return 0
          case .dangerousContent: return 1
          case .hateSpeech: return 2
          case .harassment: return 3
          case .sexuallyExplicitContent: return 4
          case .unknownIntValue(let v): return v
          case .unknownStringValue: return nil
          }
        }

        /// Returns the string value (or name) associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
        public var stringValue: String? {
          switch self {
          case .unspecified: return "SAFETY_CATEGORY_UNSPECIFIED"
          case .dangerousContent: return "DANGEROUS_CONTENT"
          case .hateSpeech: return "HATE_SPEECH"
          case .harassment: return "HARASSMENT"
          case .sexuallyExplicitContent: return "SEXUALLY_EXPLICIT_CONTENT"
          case .unknownIntValue: return nil
          case .unknownStringValue(let v): return v
          }
        }

        /// Initialize from a string value.
        ///
        /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
        public init(stringValue: String) {
          switch stringValue {
          case "SAFETY_CATEGORY_UNSPECIFIED": self = .unspecified
          case "DANGEROUS_CONTENT": self = .dangerousContent
          case "HATE_SPEECH": self = .hateSpeech
          case "HARASSMENT": self = .harassment
          case "SEXUALLY_EXPLICIT_CONTENT": self = .sexuallyExplicitContent
          default: self = .unknownStringValue(stringValue)
          }
        }

        /// Initialize from an integer value.
        ///
        /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
        public init(intValue: Int) {
          switch intValue {
          case 0: self = .unspecified
          case 1: self = .dangerousContent
          case 2: self = .hateSpeech
          case 3: self = .harassment
          case 4: self = .sexuallyExplicitContent
          default: self = .unknownIntValue(intValue)
          }
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.singleValueContainer()
          if let v = try? container.decode(Int.self) {
            self.init(intValue: v)
            return
          }
          if let s = try? container.decode(String.self) {
            if let v = Int(s) {
              self.init(intValue: v)
            } else {
              self.init(stringValue: s)
            }
            return
          }
          throw DecodingError.dataCorruptedError(
            in: container, debugDescription: "Expected enum value, must be integer or string.")
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.singleValueContainer()
          switch self {
          case .unspecified: return try container.encode(0)
          case .dangerousContent: return try container.encode(1)
          case .hateSpeech: return try container.encode(2)
          case .harassment: return try container.encode(3)
          case .sexuallyExplicitContent: return try container.encode(4)
          case .unknownIntValue(let v): return try container.encode(v)
          case .unknownStringValue(let v): return try container.encode(v)
          }
        }
      }

      public static var _anyTypeUrl: String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.SafetySettings.RaiSettings"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Settings for prompt security checks.
    public struct PromptSecuritySettings: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Optional. Enable prompt security checks.
      public var enablePromptSecurity: Swift.Bool = Swift.Bool()

      /// Initialize a new instance of `PromptSecuritySettings`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = PromptSecuritySettings().with { $0.enablePromptSecurity = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: String {
        return
          "type.googleapis.com/google.cloud.dialogflow.cx.v3.SafetySettings.PromptSecuritySettings"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Strategy for matching phrases.
    public enum PhraseMatchStrategy: Codable, Equatable, Sendable {
      /// Unspecified, defaults to PARTIAL_MATCH.
      case unspecified
      /// Text that contains the phrase as a substring will be matched, e.g. "foo"
      /// will match "afoobar".
      case partialMatch
      /// Text that contains the tokenized words of the phrase will be matched,
      /// e.g. "foo" will match "a foo bar" and "foo bar", but not "foobar".
      case wordMatch
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .partialMatch: return 1
        case .wordMatch: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: String? {
        switch self {
        case .unspecified: return "PHRASE_MATCH_STRATEGY_UNSPECIFIED"
        case .partialMatch: return "PARTIAL_MATCH"
        case .wordMatch: return "WORD_MATCH"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
      public init(stringValue: String) {
        switch stringValue {
        case "PHRASE_MATCH_STRATEGY_UNSPECIFIED": self = .unspecified
        case "PARTIAL_MATCH": self = .partialMatch
        case "WORD_MATCH": self = .wordMatch
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .partialMatch
        case 2: self = .wordMatch
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .partialMatch: return try container.encode(1)
        case .wordMatch: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: String {
      return "type.googleapis.com/google.cloud.dialogflow.cx.v3.SafetySettings"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
