// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if Tools
  import Foundation
  import GoogleCloudWkt

  /// A tool provides a list of actions which are available to the
  /// [Playbook][google.cloud.dialogflow.cx.v3.Playbook] to attain its goal. A Tool
  /// consists of a description of the tool's usage and a specification of the tool
  /// which contains the schema and authentication information.
  ///
  /// [google.cloud.dialogflow.cx.v3.Playbook]: <doc:Playbook>
  public struct Tool: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// The unique identifier of the Tool.
    /// Format:
    /// `projects/<ProjectID>/locations/<LocationID>/agents/<AgentID>/tools/<ToolID>`.
    public var name: Swift.String = Swift.String()

    /// Required. The human-readable name of the Tool, unique within an agent.
    public var displayName: Swift.String = Swift.String()

    /// Required. High level description of the Tool and its usage.
    public var description: Swift.String = Swift.String()

    /// Output only. The tool type.
    public var toolType: Tool.ToolType = Tool.ToolType()

    /// Specification of the Tool.
    public var specification: OneOf_Specification? = nil

    /// Initialize a new instance of `Tool`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Tool().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case name = "name"
      case displayName = "displayName"
      case description = "description"
      case openApiSpec = "openApiSpec"
      case dataStoreSpec = "dataStoreSpec"
      case functionSpec = "functionSpec"
      case toolType = "toolType"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.name = try container.decode(Swift.String.self, forKey: .name)
      self.displayName = try container.decode(Swift.String.self, forKey: .displayName)
      self.description = try container.decode(Swift.String.self, forKey: .description)
      self.toolType = try container.decode(Tool.ToolType.self, forKey: .toolType)

      var specification: OneOf_Specification? = nil
      let specificationCheckAndSet = {
        if specification != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'specification'"))
        }
        specification = $0
      }
      if let openApiSpec = try container.decodeIfPresent(
        Tool.OpenApiTool?.self, forKey: .openApiSpec)
      {
        try specificationCheckAndSet(.openApiSpec(openApiSpec))
      }
      if let dataStoreSpec = try container.decodeIfPresent(
        Tool.DataStoreTool?.self, forKey: .dataStoreSpec)
      {
        try specificationCheckAndSet(.dataStoreSpec(dataStoreSpec))
      }
      if let functionSpec = try container.decodeIfPresent(
        Tool.FunctionTool?.self, forKey: .functionSpec)
      {
        try specificationCheckAndSet(.functionSpec(functionSpec))
      }
      self.specification = specification
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.name, forKey: .name)
      try container.encode(self.displayName, forKey: .displayName)
      try container.encode(self.description, forKey: .description)
      try container.encode(self.toolType, forKey: .toolType)

      if let choice = self.specification {
        switch choice {
        case .openApiSpec(let value):
          try container.encode(value, forKey: .openApiSpec)
        case .dataStoreSpec(let value):
          try container.encode(value, forKey: .dataStoreSpec)
        case .functionSpec(let value):
          try container.encode(value, forKey: .functionSpec)
        }
      }
    }

    /// An OpenAPI tool is a way to provide the Tool specifications in the Open API
    /// schema format.
    public struct OpenApiTool: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Optional. Authentication information required by the API.
      public var authentication: Tool.Authentication? = nil

      /// Optional. TLS configuration for the HTTPS verification.
      public var tlsConfig: Tool.TLSConfig? = nil

      /// Optional. Service Directory configuration.
      public var serviceDirectoryConfig: Tool.ServiceDirectoryConfig? = nil

      /// Schema representation.
      public var schema: OneOf_Schema? = nil

      /// Initialize a new instance of `OpenApiTool`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = OpenApiTool().with { $0.textSchema = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case textSchema = "textSchema"
        case authentication = "authentication"
        case tlsConfig = "tlsConfig"
        case serviceDirectoryConfig = "serviceDirectoryConfig"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.authentication = try container.decodeIfPresent(
          Tool.Authentication.self, forKey: .authentication)
        self.tlsConfig = try container.decodeIfPresent(Tool.TLSConfig.self, forKey: .tlsConfig)
        self.serviceDirectoryConfig = try container.decodeIfPresent(
          Tool.ServiceDirectoryConfig.self, forKey: .serviceDirectoryConfig)

        var schema: OneOf_Schema? = nil
        let schemaCheckAndSet = {
          if schema != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'schema'"))
          }
          schema = $0
        }
        if let textSchema = try container.decodeIfPresent(Swift.String.self, forKey: .textSchema) {
          try schemaCheckAndSet(.textSchema(textSchema))
        }
        self.schema = schema
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.authentication, forKey: .authentication)
        try container.encode(self.tlsConfig, forKey: .tlsConfig)
        try container.encode(self.serviceDirectoryConfig, forKey: .serviceDirectoryConfig)

        if let choice = self.schema {
          switch choice {
          case .textSchema(let value):
            try container.encode(value, forKey: .textSchema)
          }
        }
      }

      /// Schema representation.
      public enum OneOf_Schema: Codable, Equatable, Sendable {
        /// Required. The OpenAPI schema specified as a text.
        case textSchema(Swift.String)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.OpenApiTool"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// A DataStoreTool is a way to provide specifications needed to search a
    /// list of data stores.
    public struct DataStoreTool: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Required. List of data stores to search.
      public var dataStoreConnections: [DataStoreConnection] = []

      /// Required. Fallback prompt configurations to use.
      public var fallbackPrompt: Tool.DataStoreTool.FallbackPrompt? = nil

      /// Initialize a new instance of `DataStoreTool`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = DataStoreTool().with { $0.dataStoreConnections = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// A FallbackPrompt is a way to provide specifications for the Data Store
      /// fallback prompt when generating responses.
      public struct FallbackPrompt: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Initialize a new instance of `FallbackPrompt`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = FallbackPrompt().with { $0.<placeholder> = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.DataStoreTool.FallbackPrompt"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.DataStoreTool"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// A Function tool describes the functions to be invoked on the client side.
    public struct FunctionTool: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Optional. The JSON schema is encapsulated in a
      /// [google.protobuf.Struct][google.protobuf.Struct] to describe the input of
      /// the function. This input is a JSON object that contains the function's
      /// parameters as properties of the object.
      ///
      /// [google.protobuf.Struct]: https://www.google.com/search?q=Swift+google.protobuf+GoogleCloudWkt.Struct
      public var inputSchema: GoogleCloudWkt.Struct? = nil

      /// Optional. The JSON schema is encapsulated in a
      /// [google.protobuf.Struct][google.protobuf.Struct] to describe the output
      /// of the function. This output is a JSON object that contains the
      /// function's parameters as properties of the object.
      ///
      /// [google.protobuf.Struct]: https://www.google.com/search?q=Swift+google.protobuf+GoogleCloudWkt.Struct
      public var outputSchema: GoogleCloudWkt.Struct? = nil

      /// Initialize a new instance of `FunctionTool`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = FunctionTool().with { $0.inputSchema = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.FunctionTool"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Authentication information required for API calls
    public struct Authentication: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// The auth configuration.
      public var authConfig: OneOf_AuthConfig? = nil

      /// Initialize a new instance of `Authentication`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Authentication().with { $0.apiKeyConfig = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case apiKeyConfig = "apiKeyConfig"
        case oauthConfig = "oauthConfig"
        case serviceAgentAuthConfig = "serviceAgentAuthConfig"
        case bearerTokenConfig = "bearerTokenConfig"
        case serviceAccountAuthConfig = "serviceAccountAuthConfig"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var authConfig: OneOf_AuthConfig? = nil
        let authConfigCheckAndSet = {
          if authConfig != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'authConfig'"))
          }
          authConfig = $0
        }
        if let apiKeyConfig = try container.decodeIfPresent(
          Tool.Authentication.ApiKeyConfig?.self, forKey: .apiKeyConfig)
        {
          try authConfigCheckAndSet(.apiKeyConfig(apiKeyConfig))
        }
        if let oauthConfig = try container.decodeIfPresent(
          Tool.Authentication.OAuthConfig?.self, forKey: .oauthConfig)
        {
          try authConfigCheckAndSet(.oauthConfig(oauthConfig))
        }
        if let serviceAgentAuthConfig = try container.decodeIfPresent(
          Tool.Authentication.ServiceAgentAuthConfig?.self, forKey: .serviceAgentAuthConfig)
        {
          try authConfigCheckAndSet(.serviceAgentAuthConfig(serviceAgentAuthConfig))
        }
        if let bearerTokenConfig = try container.decodeIfPresent(
          Tool.Authentication.BearerTokenConfig?.self, forKey: .bearerTokenConfig)
        {
          try authConfigCheckAndSet(.bearerTokenConfig(bearerTokenConfig))
        }
        if let serviceAccountAuthConfig = try container.decodeIfPresent(
          Tool.Authentication.ServiceAccountAuthConfig?.self, forKey: .serviceAccountAuthConfig)
        {
          try authConfigCheckAndSet(.serviceAccountAuthConfig(serviceAccountAuthConfig))
        }
        self.authConfig = authConfig
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.authConfig {
          switch choice {
          case .apiKeyConfig(let value):
            try container.encode(value, forKey: .apiKeyConfig)
          case .oauthConfig(let value):
            try container.encode(value, forKey: .oauthConfig)
          case .serviceAgentAuthConfig(let value):
            try container.encode(value, forKey: .serviceAgentAuthConfig)
          case .bearerTokenConfig(let value):
            try container.encode(value, forKey: .bearerTokenConfig)
          case .serviceAccountAuthConfig(let value):
            try container.encode(value, forKey: .serviceAccountAuthConfig)
          }
        }
      }

      /// Config for authentication with API key.
      public struct ApiKeyConfig: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. The parameter name or the header name of the API key.
        /// E.g., If the API request is "https://example.com/act?X-Api-Key=<API
        /// KEY>", "X-Api-Key" would be the parameter name.
        public var keyName: Swift.String = Swift.String()

        /// Optional. The API key. If the `secret_version_for_api_key` field is
        /// set, this field will be ignored.
        public var apiKey: Swift.String = Swift.String()

        /// Optional. The name of the SecretManager secret version resource storing
        /// the API key. If this field is set, the `api_key` field will be ignored.
        /// Format: `projects/{project}/secrets/{secret}/versions/{version}`
        public var secretVersionForApiKey: Swift.String = Swift.String()

        /// Required. Key location in the request.
        public var requestLocation: Tool.Authentication.RequestLocation = Tool.Authentication
          .RequestLocation()

        /// Initialize a new instance of `ApiKeyConfig`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = ApiKeyConfig().with { $0.keyName = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.Authentication.ApiKeyConfig"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Config for authentication with OAuth.
      public struct OAuthConfig: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. OAuth grant types.
        public var oauthGrantType: Tool.Authentication.OAuthConfig.OauthGrantType = Tool
          .Authentication.OAuthConfig.OauthGrantType()

        /// Required. The client ID from the OAuth provider.
        public var clientId: Swift.String = Swift.String()

        /// Optional. The client secret from the OAuth provider. If the
        /// `secret_version_for_client_secret` field is set, this field will be
        /// ignored.
        public var clientSecret: Swift.String = Swift.String()

        /// Optional. The name of the SecretManager secret version resource storing
        /// the client secret. If this field is set, the `client_secret` field will
        /// be ignored. Format:
        /// `projects/{project}/secrets/{secret}/versions/{version}`
        public var secretVersionForClientSecret: Swift.String = Swift.String()

        /// Required. The token endpoint in the OAuth provider to exchange for an
        /// access token.
        public var tokenEndpoint: Swift.String = Swift.String()

        /// Optional. The OAuth scopes to grant.
        public var scopes: [Swift.String] = []

        /// Initialize a new instance of `OAuthConfig`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = OAuthConfig().with { $0.oauthGrantType = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// OAuth grant types. Only [client credential
        /// grant](https://oauth.net/2/grant-types/client-credentials) is
        /// supported.
        public enum OauthGrantType: Codable, Equatable, Sendable {
          /// Default value. This value is unused.
          case unspecified
          /// Represents the [client credential
          /// flow](https://oauth.net/2/grant-types/client-credentials).
          case clientCredential
          /// Encodes an unknown integer value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownIntValue(Int)
          /// Encodes an unknown string value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownStringValue(String)

          public init() {
            self = .unspecified
          }

          /// Returns the integer value associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown string value, this returns `nil`.
          public var intValue: Int? {
            switch self {
            case .unspecified: return 0
            case .clientCredential: return 1
            case .unknownIntValue(let v): return v
            case .unknownStringValue: return nil
            }
          }

          /// Returns the string value (or name) associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
          public var stringValue: Swift.String? {
            switch self {
            case .unspecified: return "OAUTH_GRANT_TYPE_UNSPECIFIED"
            case .clientCredential: return "CLIENT_CREDENTIAL"
            case .unknownIntValue: return nil
            case .unknownStringValue(let v): return v
            }
          }

          /// Initialize from a string value.
          ///
          /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
          public init(stringValue: Swift.String) {
            switch stringValue {
            case "OAUTH_GRANT_TYPE_UNSPECIFIED": self = .unspecified
            case "CLIENT_CREDENTIAL": self = .clientCredential
            default: self = .unknownStringValue(stringValue)
            }
          }

          /// Initialize from an integer value.
          ///
          /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
          public init(intValue: Int) {
            switch intValue {
            case 0: self = .unspecified
            case 1: self = .clientCredential
            default: self = .unknownIntValue(intValue)
            }
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            if let v = try? container.decode(Int.self) {
              self.init(intValue: v)
              return
            }
            if let s = try? container.decode(String.self) {
              if let v = Int(s) {
                self.init(intValue: v)
              } else {
                self.init(stringValue: s)
              }
              return
            }
            throw DecodingError.dataCorruptedError(
              in: container, debugDescription: "Expected enum value, must be integer or string.")
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.singleValueContainer()
            switch self {
            case .unspecified: return try container.encode(0)
            case .clientCredential: return try container.encode(1)
            case .unknownIntValue(let v): return try container.encode(v)
            case .unknownStringValue(let v): return try container.encode(v)
            }
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.Authentication.OAuthConfig"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Config for auth using [Diglogflow service
      /// agent](https://cloud.google.com/iam/docs/service-agents#dialogflow-service-agent).
      public struct ServiceAgentAuthConfig: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Optional. Indicate the auth token type generated from the [Diglogflow
        /// service
        /// agent](https://cloud.google.com/iam/docs/service-agents#dialogflow-service-agent).
        /// The generated token is sent in the Authorization header.
        public var serviceAgentAuth: Tool.Authentication.ServiceAgentAuthConfig.ServiceAgentAuth =
          Tool.Authentication.ServiceAgentAuthConfig.ServiceAgentAuth()

        /// Initialize a new instance of `ServiceAgentAuthConfig`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = ServiceAgentAuthConfig().with { $0.serviceAgentAuth = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// Indicate the auth token type generated from the [Diglogflow service
        /// agent](https://cloud.google.com/iam/docs/service-agents#dialogflow-service-agent).
        public enum ServiceAgentAuth: Codable, Equatable, Sendable {
          /// Service agent auth type unspecified. Default to ID_TOKEN.
          case unspecified
          /// Use [ID
          /// token](https://cloud.google.com/docs/authentication/token-types#id)
          /// generated from service agent. This can be used to access Cloud
          /// Function and Cloud Run after you grant Invoker role to
          /// `service-<PROJECT-NUMBER>@gcp-sa-dialogflow.iam.gserviceaccount.com`.
          case idToken
          /// Use [access
          /// token](https://cloud.google.com/docs/authentication/token-types#access)
          /// generated from service agent. This can be used to access other Google
          /// Cloud APIs after you grant required roles to
          /// `service-<PROJECT-NUMBER>@gcp-sa-dialogflow.iam.gserviceaccount.com`.
          case accessToken
          /// Encodes an unknown integer value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownIntValue(Int)
          /// Encodes an unknown string value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownStringValue(String)

          public init() {
            self = .unspecified
          }

          /// Returns the integer value associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown string value, this returns `nil`.
          public var intValue: Int? {
            switch self {
            case .unspecified: return 0
            case .idToken: return 1
            case .accessToken: return 2
            case .unknownIntValue(let v): return v
            case .unknownStringValue: return nil
            }
          }

          /// Returns the string value (or name) associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
          public var stringValue: Swift.String? {
            switch self {
            case .unspecified: return "SERVICE_AGENT_AUTH_UNSPECIFIED"
            case .idToken: return "ID_TOKEN"
            case .accessToken: return "ACCESS_TOKEN"
            case .unknownIntValue: return nil
            case .unknownStringValue(let v): return v
            }
          }

          /// Initialize from a string value.
          ///
          /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
          public init(stringValue: Swift.String) {
            switch stringValue {
            case "SERVICE_AGENT_AUTH_UNSPECIFIED": self = .unspecified
            case "ID_TOKEN": self = .idToken
            case "ACCESS_TOKEN": self = .accessToken
            default: self = .unknownStringValue(stringValue)
            }
          }

          /// Initialize from an integer value.
          ///
          /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
          public init(intValue: Int) {
            switch intValue {
            case 0: self = .unspecified
            case 1: self = .idToken
            case 2: self = .accessToken
            default: self = .unknownIntValue(intValue)
            }
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            if let v = try? container.decode(Int.self) {
              self.init(intValue: v)
              return
            }
            if let s = try? container.decode(String.self) {
              if let v = Int(s) {
                self.init(intValue: v)
              } else {
                self.init(stringValue: s)
              }
              return
            }
            throw DecodingError.dataCorruptedError(
              in: container, debugDescription: "Expected enum value, must be integer or string.")
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.singleValueContainer()
            switch self {
            case .unspecified: return try container.encode(0)
            case .idToken: return try container.encode(1)
            case .accessToken: return try container.encode(2)
            case .unknownIntValue(let v): return try container.encode(v)
            case .unknownStringValue(let v): return try container.encode(v)
            }
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.Authentication.ServiceAgentAuthConfig"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Config for authentication using bearer token.
      public struct BearerTokenConfig: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Optional. The text token appended to the text `Bearer` to the request
        /// Authorization header.
        /// [Session parameters
        /// reference](https://cloud.google.com/dialogflow/cx/docs/concept/parameter#session-ref)
        /// can be used to pass the token dynamically, e.g.
        /// `$session.params.parameter-id`.
        public var token: Swift.String = Swift.String()

        /// Optional. The name of the SecretManager secret version resource storing
        /// the Bearer token. If this field is set, the `token` field will be
        /// ignored. Format:
        /// `projects/{project}/secrets/{secret}/versions/{version}`
        public var secretVersionForToken: Swift.String = Swift.String()

        /// Initialize a new instance of `BearerTokenConfig`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = BearerTokenConfig().with { $0.token = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.Authentication.BearerTokenConfig"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Configuration for authentication using a service account.
      public struct ServiceAccountAuthConfig: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. The email address of the service account used to authenticate
        /// the tool call. Dialogflow uses this service account to exchange an
        /// access token and the access token is then sent in the `Authorization`
        /// header of the tool request.
        ///
        /// The service account must have the
        /// `roles/iam.serviceAccountTokenCreator` role granted to the
        /// [Dialogflow service
        /// agent](https://cloud.google.com/iam/docs/service-agents#dialogflow-service-agent).
        public var serviceAccount: Swift.String = Swift.String()

        /// Initialize a new instance of `ServiceAccountAuthConfig`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = ServiceAccountAuthConfig().with { $0.serviceAccount = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.Authentication.ServiceAccountAuthConfig"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The location of the API key in the request.
      public enum RequestLocation: Codable, Equatable, Sendable {
        /// Default value. This value is unused.
        case unspecified
        /// Represents the key in http header.
        case header
        /// Represents the key in query string.
        case queryString
        /// Encodes an unknown integer value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownIntValue(Int)
        /// Encodes an unknown string value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownStringValue(String)

        public init() {
          self = .unspecified
        }

        /// Returns the integer value associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown string value, this returns `nil`.
        public var intValue: Int? {
          switch self {
          case .unspecified: return 0
          case .header: return 1
          case .queryString: return 2
          case .unknownIntValue(let v): return v
          case .unknownStringValue: return nil
          }
        }

        /// Returns the string value (or name) associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
        public var stringValue: Swift.String? {
          switch self {
          case .unspecified: return "REQUEST_LOCATION_UNSPECIFIED"
          case .header: return "HEADER"
          case .queryString: return "QUERY_STRING"
          case .unknownIntValue: return nil
          case .unknownStringValue(let v): return v
          }
        }

        /// Initialize from a string value.
        ///
        /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
        public init(stringValue: Swift.String) {
          switch stringValue {
          case "REQUEST_LOCATION_UNSPECIFIED": self = .unspecified
          case "HEADER": self = .header
          case "QUERY_STRING": self = .queryString
          default: self = .unknownStringValue(stringValue)
          }
        }

        /// Initialize from an integer value.
        ///
        /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
        public init(intValue: Int) {
          switch intValue {
          case 0: self = .unspecified
          case 1: self = .header
          case 2: self = .queryString
          default: self = .unknownIntValue(intValue)
          }
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.singleValueContainer()
          if let v = try? container.decode(Int.self) {
            self.init(intValue: v)
            return
          }
          if let s = try? container.decode(String.self) {
            if let v = Int(s) {
              self.init(intValue: v)
            } else {
              self.init(stringValue: s)
            }
            return
          }
          throw DecodingError.dataCorruptedError(
            in: container, debugDescription: "Expected enum value, must be integer or string.")
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.singleValueContainer()
          switch self {
          case .unspecified: return try container.encode(0)
          case .header: return try container.encode(1)
          case .queryString: return try container.encode(2)
          case .unknownIntValue(let v): return try container.encode(v)
          case .unknownStringValue(let v): return try container.encode(v)
          }
        }
      }

      /// The auth configuration.
      public enum OneOf_AuthConfig: Codable, Equatable, Sendable {
        /// Config for API key auth.
        indirect case apiKeyConfig(Tool.Authentication.ApiKeyConfig?)
        /// Config for OAuth.
        indirect case oauthConfig(Tool.Authentication.OAuthConfig?)
        /// Config for [Diglogflow service
        /// agent](https://cloud.google.com/iam/docs/service-agents#dialogflow-service-agent)
        /// auth.
        indirect case serviceAgentAuthConfig(Tool.Authentication.ServiceAgentAuthConfig?)
        /// Config for bearer token auth.
        indirect case bearerTokenConfig(Tool.Authentication.BearerTokenConfig?)
        /// Configuration for service account authentication.
        indirect case serviceAccountAuthConfig(Tool.Authentication.ServiceAccountAuthConfig?)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.Authentication"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// The TLS configuration.
    public struct TLSConfig: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Required. Specifies a list of allowed custom CA certificates for HTTPS
      /// verification.
      public var caCerts: [Tool.TLSConfig.CACert] = []

      /// Initialize a new instance of `TLSConfig`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = TLSConfig().with { $0.caCerts = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// The CA certificate.
      public struct CACert: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. The name of the allowed custom CA certificates. This
        /// can be used to disambiguate the custom CA certificates.
        public var displayName: Swift.String = Swift.String()

        /// Required. The allowed custom CA certificates (in DER format) for
        /// HTTPS verification. This overrides the default SSL trust store. If this
        /// is empty or unspecified, Dialogflow will use Google's default trust
        /// store to verify certificates. N.B. Make sure the HTTPS server
        /// certificates are signed with "subject alt name". For instance a
        /// certificate can be self-signed using the following command:
        ///
        /// ```
        ///    openssl x509 -req -days 200 -in example.com.csr \
        ///      -signkey example.com.key \
        ///      -out example.com.crt \
        ///      -extfile <(printf "\nsubjectAltName='DNS:www.example.com'")
        /// ```
        public var cert: Foundation.Data = Foundation.Data()

        /// Initialize a new instance of `CACert`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = CACert().with { $0.displayName = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.TLSConfig.CACert"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.TLSConfig"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Configuration for tools using Service Directory.
    public struct ServiceDirectoryConfig: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Required. The name of [Service
      /// Directory](https://cloud.google.com/service-directory) service.
      /// Format:
      /// `projects/<ProjectID>/locations/<LocationID>/namespaces/<NamespaceID>/services/<ServiceID>`.
      /// `LocationID` of the service directory must be the same as the location
      /// of the agent.
      public var service: Swift.String = Swift.String()

      /// Initialize a new instance of `ServiceDirectoryConfig`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = ServiceDirectoryConfig().with { $0.service = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool.ServiceDirectoryConfig"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Represents the type of the tool.
    public enum ToolType: Codable, Equatable, Sendable {
      /// Default value. This value is unused.
      case unspecified
      /// Customer provided tool.
      case customizedTool
      /// First party built-in tool created by Dialogflow which cannot be modified.
      case builtinTool
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .customizedTool: return 1
        case .builtinTool: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "TOOL_TYPE_UNSPECIFIED"
        case .customizedTool: return "CUSTOMIZED_TOOL"
        case .builtinTool: return "BUILTIN_TOOL"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "TOOL_TYPE_UNSPECIFIED": self = .unspecified
        case "CUSTOMIZED_TOOL": self = .customizedTool
        case "BUILTIN_TOOL": self = .builtinTool
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .customizedTool
        case 2: self = .builtinTool
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .customizedTool: return try container.encode(1)
        case .builtinTool: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Specification of the Tool.
    public enum OneOf_Specification: Codable, Equatable, Sendable {
      /// OpenAPI specification of the Tool.
      indirect case openApiSpec(Tool.OpenApiTool?)
      /// Data store search tool specification.
      indirect case dataStoreSpec(Tool.DataStoreTool?)
      /// Client side executed function specification.
      indirect case functionSpec(Tool.FunctionTool?)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dialogflow.cx.v3.Tool"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
